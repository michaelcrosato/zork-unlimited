# AI Loop State

### Cycle result — content_fix / WHITE_STAG TARN POST-KNOWS_TRUTH NOW REFLECTS STONE ALREADY READ (bug_0329): **Blind pass on `white_stag`** (seed 7; fresh MCP-only subagent; report `ai-runs/2026-06-08T16-30-09-430Z/playtest.md`): **STRONG — clarity 5/5, enjoyment 5/5**, 4 playthroughs + 2 variant checks, all 4 endings reached (ending_quarry [knows+ignorant] · ending_lost · ending_thaw [knows+ignorant] · ending_offering), zero rejected actions, mechanically clean. ONE concrete actionable bug: tarn post-knows_truth variant closed with "the leaning stone keeps its old vow at the bank" — implying the stone is still visitable — but `read_stone` is gated on `not_flag: knows_truth` and was absent from the action list. Prose overpromised an unavailable action (the only text/action mismatch in the pack). **FIX (content, pure prose):** changed the closing of the knows_truth tarn variant from "keeps its old vow at the bank" → "You have read the stone; its vow is in you now." Prose-only; no flag, choice, route, or ending change. **Self-critique: real (if minor) player-facing mismatch — prose implied re-visit, action absent; pack scores 5/5 on both dimensions; this class (single-instance prose/action mismatch in a reads-clean pack) is a one-line fix; no higher-value structural move warranted. Not busywork.** **Locked:** `tests/regression/white_stag_tarn_stone_reactive.test.ts` (4 cases: stale text absent after read; new text present; read_stone absent; base text still shows on first visit). `traces/bugs/bug_0329_white_stag_tarn_stone_stale_text.yaml`. **VERIFY: `npm run health` EXIT 0 — 2099 tests (+4 from 2095), all 17 packs validate 0/0; verify:integrity 0/0; lint/format clean.** Next: rotate blind pass to `wolf_winter_v1` (next in assessor rotation).
## AFK Cycle 2026-06-08T16-30-09-430Z
- Assessment: packs cyoa=7 parser=5 rpg=5; 17 candidate(s) ranked.
- Next best improvement (recommended): [content_fix] Blind-playtest "white_stag_v1" — structurally clean; only a fresh blind LLM player can judge its quality.
- Why: The validator and exhaustive solver prove this pack is winnable and sound; only a fresh blind LLM playtest reveals signposting/clarity/pacing issues a static check can't see.
- Mandatory LLM playtest target this cycle: content/cyoa/pack/white_stag.yaml.
- Process: assessor ranks → blind LLM playtest for quality → one improvement → health + verify:integrity green → commit (trust-but-verify).

### Cycle result — content_fix / TITHE_BARN LEAVE_BOOK NOW SETS KNOWS_TRUTH (bug_0328): **Blind pass on `tithe_barn`** (seed 7; fresh MCP-only subagent; report `ai-runs/2026-06-08T16-18-46-254Z/playtest.md`): **STRONG — clarity 5/5, enjoyment 5/5**, 4 playthroughs, all 4 endings reached (ending_fall · ending_bounty [base+KNOWING] · ending_mercy [base+haunted] · ending_doors), zero rejected actions, mechanically clean. ONE concrete actionable bug: reading the ledger base text (the full damning revelation — clerk's hand, lord's seal, grain held to rot) then choosing `leave_book` silently exited without setting `knows_truth` — `open_doors` absent at reckoning, endings showed base text instead of reactive variants. Player who "read and acted" was treated as uninformed, the pack's core moral engine quietly misfired. **FIX (content, effects only):** added `set_flag: knows_truth` + `set_quest_stage: truth_read` to `leave_book` in the `ledger` scene; no `add_journal` (decipher retains the journal entry; effects are no-ops on second visit when flag already set). No label, route, or ending change. **Self-critique: real player-facing gap (blind test primary finding); fix closes it with 3 YAML lines; pack 5/5 on both dimensions; no higher-value structural move identified — this class (silently-skippable two-step gate) has one confirmed instance, not worth a validator rule yet. Not busywork.** **Locked:** `tests/regression/tithe_barn_ledger_leave_sets_truth.test.ts` (6 cases: leave_no_decipher → open_doors present; ending_doors reachable; KNOWING bounty variant fires; haunted mercy variant fires; decipher path unchanged; double-leave no-op). `traces/bugs/bug_0328_tithe_barn_ledger_leave_no_truth.yaml`. **VERIFY: `npm run health` EXIT 0 — 2095 tests (+6 from 2089), all 17 packs validate 0/0; verify:integrity 0/0; lint/format clean.** Next: rotate blind pass to `wolf_winter_v1` (next in assessor rotation).
## AFK Cycle 2026-06-08T16-18-46-254Z
- Assessment: packs cyoa=7 parser=5 rpg=5; 17 candidate(s) ranked.
- Next best improvement (recommended): [content_fix] Blind-playtest "tithe_barn_v1" — structurally clean; only a fresh blind LLM player can judge its quality.
- Why: The validator and exhaustive solver prove this pack is winnable and sound; only a fresh blind LLM playtest reveals signposting/clarity/pacing issues a static check can't see.
- Mandatory LLM playtest target this cycle: content/cyoa/pack/tithe_barn.yaml.
- Process: assessor ranks → blind LLM playtest for quality → one improvement → health + verify:integrity green → commit (trust-but-verify).

### Cycle result — content_fix / DEAD_RECKONING BACK_DECK NOW SHOWS AT_THE_CASK DECK VARIANTS (bug_0327): **Blind pass on `dead_reckoning`** (seed 7; fresh MCP-only subagent; report `ai-runs/2026-06-08T16-05-59-495Z/playtest.md`): **STRONG — clarity 5/5, enjoyment 5/5**, all 5 endings reached (ending_landfall · ending_jonah · ending_holdfast · ending_adrift · ending_irons), zero rejected actions, mechanically clean. ONE concrete actionable bug: after `back_deck` (retreat from cask), the deck rendered the zero-quest-stage opening text verbatim — no acknowledgment that the player had just faced Tarrant and stepped away. All other state transitions tracked scrupulously; this gap briefly broke the fiction. Design Flaw 2 (`give_jonah` no knows_pilot epilogue) was a FALSE ALARM — ending_jonah lines 417-428 already have the reactive `knows_pilot` variant; subagent's jonah run was pre-knowledge. **FIX (content, pure prose):** added 4 variants gated on `quest_stage: at_the_cask` to the deck scene (both/course/pilot/bare — same 2×2 as adrift window), placed before existing adrift variants (first-match-wins; stages mutually exclusive). Each opens "You stepped back from the cask…" and is colored by what the player knows. Base text now fires only for first-turn (quest unset). No flag/score/choice/ending change. **Self-critique: real player-facing gap (blind test called it out directly); fix closes it without structural change; pack scored 5/5 on both dimensions so no higher-value structural move identified. Not busywork.** **Locked:** `tests/regression/dead_reckoning_back_deck_reactive.test.ts` (6 cases: bare back_deck → new text/no opening; course back_deck → course variant; pilot back_deck → pilot variant; both back_deck → full-knowledge variant; first turn → opening text still fires; ending_landfall reachable post-detour). `traces/bugs/bug_0327_dead_reckoning_back_deck_stale_text.yaml`. **VERIFY: `npm run health` EXIT 0 — 2089 tests (+6 from 2083), all 17 packs validate 0/0; verify:integrity 0/0; lint/format clean.** Next: rotate blind pass to `wolf_winter_v1` (next in assessor rotation).
## AFK Cycle 2026-06-08T16-05-59-495Z
- Assessment: packs cyoa=7 parser=5 rpg=5; 17 candidate(s) ranked.
- Next best improvement (recommended): [content_fix] Blind-playtest "dead_reckoning_v1" — structurally clean; only a fresh blind LLM player can judge its quality.
- Why: The validator and exhaustive solver prove this pack is winnable and sound; only a fresh blind LLM playtest reveals signposting/clarity/pacing issues a static check can't see.
- Mandatory LLM playtest target this cycle: content/cyoa/pack/dead_reckoning.yaml.
- Process: assessor ranks → blind LLM playtest for quality → one improvement → health + verify:integrity green → commit (trust-but-verify).

### Cycle result — content_fix / CLOCKWORK_HEIST CRAWLSPACE BOUNCE-BACK NOW NAMES THE STUDY (bug_0326): **Blind pass on `clockwork_heist`** (seed 7; fresh MCP-only subagent; report `ai-runs/2026-06-08T15-44-14-673Z/playtest.md`): **STRONG — clarity 5/5, enjoyment 4/5**, 4 playthroughs, all 4 endings reached (ending_truth ×2 [hidden 20/45 · displayed 45/45] · ending_rich 45/45 · ending_caught 0/45), zero rejected actions, mechanically clean. ONE concrete actionable bug: `study_strongbox` bounce-back narrated "Better search the manor for it before you crawl back here" — sending first-time crawlspace players straight to the kitchen and back, scoring 20/45 at ending_truth. The design intends crawlspace max = 30 (ledger +10 + confession +20), but the bounce-back gave no hint the study was worth visiting. Ticks-deadline and guile findings are by-design (false alarms). **FIX (content, pure prose):** changed `study_strongbox` narrate closing from "search the manor for it before you crawl back here" → "search the manor for it: the kitchens are off the foyer below, and the steward's study waits up the stair." Both destinations now named explicitly; crawlspace-first players naturally discover the ledger and reach the intended 30/45. No flag, score, choice, routing, or ending change. **Self-critique: real player-facing gap (20/45 for complete ending); prose signpost directly closes it; no higher-value structural move identified. Not busywork.** **Locked:** `tests/regression/clockwork_crawlspace_study_signpost.test.ts` (4 cases: narrate references study+kitchen; without-ledger route → 20/45; with-ledger route → 30/45; both → ending_truth). `traces/bugs/bug_0326_crawlspace_study_signpost.yaml`. **VERIFY: `npm run health` EXIT 0 — 2083 tests (+4 from 2079), all 17 packs validate 0/0; verify:integrity 0/0; lint/format clean.** Next: rotate blind pass to `wolf_winter_v1` (next in assessor rotation).
## AFK Cycle 2026-06-08T15-44-14-673Z
- Assessment: packs cyoa=7 parser=5 rpg=5; 17 candidate(s) ranked.
- Next best improvement (recommended): [content_fix] Blind-playtest "clockwork_heist_v1" — structurally clean; only a fresh blind LLM player can judge its quality.
- Why: The validator and exhaustive solver prove this pack is winnable and sound; only a fresh blind LLM playtest reveals signposting/clarity/pacing issues a static check can't see.
- Mandatory LLM playtest target this cycle: content/cyoa/pack/clockwork_heist.yaml.
- Process: assessor ranks → blind LLM playtest for quality → one improvement → health + verify:integrity green → commit (trust-but-verify).

### Cycle result — content_fix / SUNKEN_BARROW REAVER'S SHADE NO LONGER RE-GREETS ON EVERY RETURN (bug_0325): **Blind pass on `sunken_barrow`** (seed 7; fresh MCP-only subagent; report `ai-runs/2026-06-08T14-06-03-284Z/playtest.md`): **STRONG — clarity 5/5, enjoyment 4/5**, 2 playthroughs, ending_victory 50/50 (full prep) + ending_woken 25/50 (sarcophagus doom), zero rejected actions, mechanically clean. ONE concrete actionable bug: `shade_root` re-fired the full first-meeting greeting ("Another one, come down for the cold crown…") on every return from a topic branch — "Another one" implies first contact each time, jarring mid-conversation. Same dialogue-regreet class as bug_0309/bug_0322. Other findings: sarcophagus examine conditional reference to shade's warning (functional workaround "if you stopped to hear it" — deferred); score 25/50 on doom and incomplete runs (by-design). **FIX (content, pure prose):** added `variants` block to `shade_root` with `any_of[heard_warding, heard_lord_lore]` → "What else would you ask of me?" First visit (no flags) unchanged. No flag, score, exit, or ending change. Re-pinned content hash in `traces/rpg/barrow_victory.json`. **Locked:** `tests/regression/sunken_barrow_shade_root_short_greeting.test.ts` (4 cases: first visit → full greeting; after wight topic → short form; after lord topic → short form; win route → ending_victory 50/50). `traces/bugs/bug_0325_sunken_barrow_shade_root_regreet.yaml`. **VERIFY: `npm run health` EXIT 0 — 2079 tests (+4 from 2075), 328 files; all 17 packs validate 0/0; verify:integrity 0/0; lint/format clean.** Next: rotate blind pass to `wolf_winter_v1` (next in assessor rotation).
## AFK Cycle 2026-06-08T14-06-03-284Z
- Assessment: packs cyoa=7 parser=5 rpg=5; 17 candidate(s) ranked.
- Next best improvement (recommended): [content_fix] Blind-playtest "sunken_barrow_v1" — structurally clean; only a fresh blind LLM player can judge its quality.
- Why: The validator and exhaustive solver prove this pack is winnable and sound; only a fresh blind LLM playtest reveals signposting/clarity/pacing issues a static check can't see.
- Mandatory LLM playtest target this cycle: content/rpg/pack/sunken_barrow.yaml.
- Process: assessor ranks → blind LLM playtest for quality → one improvement → health + verify:integrity green → commit (trust-but-verify).

### Cycle result — content_fix / DAWN_BEACON CROWN NO LONGER CLAIMS FLINT IN BED AFTER TAKEN (bug_0324): **Blind pass on `dawn_beacon`** (seed 7; fresh MCP-only subagent; report `ai-runs/2026-06-08T13-56-45-884Z/playtest.md`): **STRONG — clarity 5/5, enjoyment 4/5**, 2 playthroughs, ending_lit 50/50 (full prep) + ending_lit 45/50 (no prep), zero rejected actions, mechanically clean. ONE concrete actionable bug: `beacon_crown` room description said "the watch's own flint lies in the bed beside it" after the player had picked up the flint — visible_objects updated correctly but the prose did not. Same reactive-description-blindness class as bugs 0282–0323. Other findings: single ending regardless of preparation level (by-design score differentiation); garrison mail silently undonned (design flaw, deferred); watchman talk persists post-conversation (by-design). **FIX (content, pure prose):** added `variants` block to `beacon_crown` with `when: [{ has_item: flint }]` guard → "The flint is in your hand — one struck spark to wake the valley." Base description (flint not yet taken) unchanged. No flag, score, exit, or ending change. **Locked:** `tests/regression/dawn_beacon_crown_flint_reactive.test.ts` (4 cases: no flint → stale text present; has flint → stale text absent; has flint → new text present; pack validates + ending_lit defined). `traces/bugs/bug_0324_dawn_beacon_crown_flint_stale_text.yaml`. **VERIFY: `npm run health` EXIT 0 — 2075 tests (+8 from 2067), 327 files; all 17 packs validate 0/0; verify:integrity 0/0; lint/format clean.** Next: rotate blind pass to `sunken_barrow_v1` (next in assessor rotation).
## AFK Cycle 2026-06-08T13-56-45-884Z
- Assessment: packs cyoa=7 parser=5 rpg=5; 17 candidate(s) ranked.
- Next best improvement (recommended): [content_fix] Blind-playtest "dawn_beacon_v1" — structurally clean; only a fresh blind LLM player can judge its quality.
- Why: The validator and exhaustive solver prove this pack is winnable and sound; only a fresh blind LLM playtest reveals signposting/clarity/pacing issues a static check can't see.
- Mandatory LLM playtest target this cycle: content/rpg/pack/dawn_beacon.yaml.
- Process: assessor ranks → blind LLM playtest for quality → one improvement → health + verify:integrity green → commit (trust-but-verify).

### Cycle result — content_fix / COLD_FORGE SPIRIT_ROOT NO LONGER RE-SHOWS FULL GREETING ON RETURN (bug_0322): **Blind pass on `cold_forge`** (seed 7; fresh MCP-only subagent; report `ai-runs/2026-06-08T13-34-50-175Z/playtest.md`): **STRONG — clarity 5/5, enjoyment 4/5**, single playthrough, ending_victory 50/50, zero rejected actions, mechanically clean. ONE concrete actionable finding: the `spirit_root` dialogue node re-showed the full first-meeting greeting ("A warm thing, come down into the cold...") on every return from a topic node — a player who asked all four topics saw the greeting five times, adding four mechanical repetitions with no new content and breaking the conversational illusion. Same dialogue-regreet class as the hermit_camp fix (bug_0309). Other findings: sentinel ATK stat not surfaced (by-design per bug_0101), hammer false-red-herring (already addressed per bug_0088 flavour text), no early score (by-design: prep not points), failure endings exist (ending_fallen) but not encountered. **FIX (content, pure prose):** added `variants` block to `spirit_root` with one variant: `any_of[heard_sentinel, heard_heart, heard_forge, heard_founder]` → "What else would you know?" First visit unchanged (no flags set); subsequent returns show short acknowledgment. No new flag, no score/route/stat/ending change. **Locked:** `tests/regression/cold_forge_spirit_root_short_greeting.test.ts` (4 cases: first visit → full greeting; return after sentinel → short form; return after heart → short form; win route → ending_victory 50/50). `traces/bugs/bug_0322_cold_forge_spirit_root_regreet.yaml`. **VERIFY: `npm run health` EXIT 0 — 2067 tests (+4 from 2063), 317 files; all 17 packs validate 0/0; verify:integrity 0/0; lint/format clean.** Next: rotate blind pass to `dawn_beacon_v1` (next in assessor rotation).
## AFK Cycle 2026-06-08T13-34-50-175Z
- Assessment: packs cyoa=7 parser=5 rpg=5; 17 candidate(s) ranked.
- Next best improvement (recommended): [content_fix] Blind-playtest "cold_forge_v1" — structurally clean; only a fresh blind LLM player can judge its quality.
- Why: The validator and exhaustive solver prove this pack is winnable and sound; only a fresh blind LLM playtest reveals signposting/clarity/pacing issues a static check can't see.
- Mandatory LLM playtest target this cycle: content/rpg/pack/cold_forge.yaml.
- Process: assessor ranks → blind LLM playtest for quality → one improvement → health + verify:integrity green → commit (trust-but-verify).

### Cycle result — content_fix / BREAKING_WEIR STORM-WALK NO LONGER SHOWS "CLIP ON" WHEN LIFE-LINE NOT HELD (bug_0321): **Blind pass on `breaking_weir`** (seed 7; fresh MCP-only subagent; report `ai-runs/2026-06-08T13-20-49-598Z/playtest.md`): **STRONG — clarity 5/5, enjoyment 4/5**, 3 playthroughs, ending_held 50/50 (full route), ending_held 45/50 (rush, no book/Pell), no-rope dead-end confirmed recoverable; zero rejected actions, mechanically clean. ONE concrete actionable bug: the `weir_walk` room description said "clip on" (base: "clip on and go steady"; uncounselled variant: "clip on and you may yet make it") regardless of whether the life-line was in the player's inventory — same reactive-description-blindness class as bug_0282/0287/0302/0307. Player who forgot the rope saw the hint but no crossing action appeared. **FIX (content, pure prose):** added `not_item: life_line` variant to `weir_walk` room, ordered after `walk_crossed` and before `not_flag:heard_walk` (first-match-wins). No-rope text replaces "clip on" with "no life-line — without the rope snapped to the wire the crossing cannot be made. The rope hangs on its peg back in the lodge." No flag/score/exit/ending change. Updated `breaking_weir_gamble_legible.test.ts` to pass life_line in inventory for gamble-cue tests (so not_item variant doesn't preempt them). **Locked:** `tests/regression/breaking_weir_walk_no_rope_reactive.test.ts` (5 cases: no rope → no-rope text / no "clip on" / no "raw nerve" gamble cue; has rope + no counsel → "raw nerve" present; walk_crossed → crossed variant wins regardless). `traces/bugs/bug_0321_breaking_weir_walk_no_rope_clip_hint.yaml`. **VERIFY: `npm run health` EXIT 0 — 2063 tests (+5 from 2025 +updated gamble_legible), 316 files; all 17 packs validate 0/0; verify:integrity 0/0; lint/format clean.** Next: rotate blind pass to `cold_forge_v1` (next in assessor rotation).
## AFK Cycle 2026-06-08T13-20-49-598Z
- Assessment: packs cyoa=7 parser=5 rpg=5; 17 candidate(s) ranked.
- Next best improvement (recommended): [content_fix] Blind-playtest "breaking_weir_v1" — structurally clean; only a fresh blind LLM player can judge its quality.
- Why: The validator and exhaustive solver prove this pack is winnable and sound; only a fresh blind LLM playtest reveals signposting/clarity/pacing issues a static check can't see.
- Mandatory LLM playtest target this cycle: content/rpg/pack/breaking_weir.yaml.
- Process: assessor ranks → blind LLM playtest for quality → one improvement → health + verify:integrity green → commit (trust-but-verify).

### Cycle result — content_fix / MIDNIGHT_EDITION SKILL CHECK NOW LEAVES VISIBLE PERSISTENT TRACE IN ALLEY SCENE (bug_0313): **Mandated blind pass ran on `midnight_edition`** (seed 7; fresh MCP-only subagent; report `ai-runs/2026-06-08T11-14-59-656Z/playtest.md`): **STRONG — clarity 5/5, enjoyment 4/5**, 4 full playthroughs, all 4 endings reached (ending_vindicated 35/35 · ending_silenced · ending_spiked · ending_libel), zero rejected actions, mechanically flawless. Previously-fixed bugs (bug_0310, bug_0312) confirmed absent. ONE concrete actionable finding: the `steady_and_bar` skill check (alley_door) set only `door_barred` on both branches — identical to the plain `bar_door` choice — so re-entering the alley showed byte-identical prose ("dropped hard into its brackets where you slammed it") whether the player had wrestled the bar home against a lifting latch or just dropped it calmly. Same reactive-description-blindness class as bug_0305 (sealed_crypt grip beat with no reader). Convergence constraint: `cyoa_convergent_skill_check_sound.test.ts` requires `stateKey(success) == stateKey(failure)` — adding distinguishing flags (`nerve_held`/`nerve_shook`) would break this; instead added the SAME `nerve_check_attempted` flag to BOTH branches (convergence preserved). **FIX (content, pure prose + one convergent flag):** added `set_flag: nerve_check_attempted` to both `on_success` and `on_failure` of `steady_and_bar`; added alley_door variant `{door_barred AND nerve_check_attempted}` (placed before plain `{door_barred}`) showing "thrown into its brackets with the latch already moving in the frame when you got it home" — accurate for both success (clean, under pressure) and failure (barely in time); plain `{door_barred}` now fires only for `bar_door`. Flag read ONLY by this prose variant — no choice condition, score, or route reads it; convergence and all exhaustive proofs stay sound. **Locked:** `tests/regression/midnight_edition_skill_check_reactive.test.ts` (6 cases: skill-check re-entry → "latch moving" text; bar_door re-entry → "dropped hard"; nerve_check_attempted set on both outcomes; not set after bar_door; no choice gating; ending_vindicated 35/35). `traces/bugs/bug_0313_midnight_skill_check_no_persistent_trace.yaml`. **VERIFY: `npm run health` EXIT 0 — 2025 tests (+21 from 2004), 316 files; all 17 packs validate 0/0; verify:integrity 0/0; lint/format clean.** Next: rotate blind pass to `breaking_weir_v1` (next in assessor rotation).
## AFK Cycle 2026-06-08T11-14-59-656Z
- Assessment: packs cyoa=7 parser=5 rpg=5; 17 candidate(s) ranked.
- Next best improvement (recommended): [content_fix] Blind-playtest "midnight_edition_v1" — structurally clean; only a fresh blind LLM player can judge its quality.
- Why: The validator and exhaustive solver prove this pack is winnable and sound; only a fresh blind LLM playtest reveals signposting/clarity/pacing issues a static check can't see.
- Mandatory LLM playtest target this cycle: content/cyoa/pack/midnight_edition.yaml.
- Process: assessor ranks → blind LLM playtest for quality → one improvement → health + verify:integrity green → commit (trust-but-verify).

### Cycle result — content_fix / WATCHTOWER HERMIT_CAMP NO LONGER SHOWS FIRST-MEETING TEXT AFTER CONVERSATION (bug_0309): blind pass on `watchtower_road` (seed 7; fresh MCP-only subagent; report `ai-runs/2026-06-08T10-25-38-070Z/playtest.md`): **STRONG — clarity 4/5, enjoyment 4/5**, 3 playthroughs, all 3 endings reached (ending_truth ×2 [base 45/50 · raised_alarm 40/50] · ending_escape), zero rejected actions, mechanically flawless. ONE concrete actionable bug (B1): returning to `hermit_camp` after full conversation (heard_hermit_lore or seal_broken) still showed "He watches you with pale, unsurprised eyes" — `hermit_talk` has a reactive variant (bug_0134) and `mossy_brook` mirrors it (bug_0282); `hermit_camp` (outer landing) was the missed member of the trio. Other findings: nav backtrack from cellar to north road (B2, navigation friction, not a reactive-description bug); crossroads static smoke text (B3, minor); score 45/50 on natural route without letter-first ordering (B4/B5, design discoverability, not a data bug). **FIX (content, pure prose):** added `variants:` to `hermit_camp` with `any_of[heard_hermit_lore, seal_broken]` guard (identical to hermit_talk + mossy_brook): "no stranger to him now." Prose only — no flag/choice/route/ending change. Re-pinned content hash (was 9fed38...). **Locked:** `tests/regression/watchtower_hermit_camp_reentry.test.ts` (4 cases: first visit → "pale, unsurprised" / after lore → "no stranger" / after seal_broken only → "no stranger" / choices unchanged + ending_truth 50/50 reachable). `traces/bugs/bug_0309_watchtower_hermit_camp_stale_reentry.yaml`. **VERIFY: `npm run health` EXIT 0 — 2004 tests (+4 from 2000), 312 files; all 17 packs validate 0/0; verify:integrity 0/0; lint/format clean.** Next: rotate blind pass to `breaking_weir_v1` (next in assessor rotation); structural runner-up = `ITEM_UNPLACED` validator or tide_mill stale item descriptions (tool_shed/wheel_room, 3 candidates from previous blind pass).
## AFK Cycle 2026-06-08T10-25-38-070Z
- Assessment: packs cyoa=7 parser=5 rpg=5; 17 candidate(s) ranked.
- Next best improvement (recommended): [content_fix] Blind-playtest "watchtower_road_v1" — structurally clean; only a fresh blind LLM player can judge its quality.
- Why: The validator and exhaustive solver prove this pack is winnable and sound; only a fresh blind LLM playtest reveals signposting/clarity/pacing issues a static check can't see.
- Mandatory LLM playtest target this cycle: content/cyoa/pack/watchtower_road.yaml.
- Process: assessor ranks → blind LLM playtest for quality → one improvement → health + verify:integrity green → commit (trust-but-verify).

### Cycle result — ULTRAPLAN RE-AIM #18 / VACUOUS-ASSERTION DETECTOR LOCKED (bug_0308): assessor saturated (0.5-floor blind stubs). **Bounded ultraplan** (4 repo reviewers [engine/verify-integrity · content/validator · loop/strategy · verification/benchmark] + 2 web researchers → 1 synthesis; 7 agents). **Two false alarms confirmed at source:** BFS forward-reachability already implemented at `parser_validator.ts` lines 339-350 (`UNREACHABLE_ROOM`); `hide_graph` per-call override already landed (bug_0299). **Three genuine gaps converged:** (1) vacuous-assertion tautology detector — script's own docstring at lines 31-33 admits the gap; (2) `ITEM_UNPLACED` validator (no orphan-item code anywhere); (3) `TARGET_PER_MODE` stagnation (7/5/5 vs 2/2/2 threshold, `content_new` permanently silenced). **Chose #1:** S-effort, no API key, closes the EvilGenie (arXiv:2511.21654) count-preserving launder the three-count system admits; SpecBench (arXiv:2605.21384) confirms two-layer detection is strongest only when the deterministic layer is complete. **FIX (scripts/verify-integrity.ts):** `TAUTOLOGY_RE` (backreference captures identical actual+expected), `detectTautologies()` → `TAUTOLOGY_ASSERTION` + `TAUTOLOGY_FLOOR` in `runStatic()`; `countTautologyAssertions()` + `TAUTOLOGY_REGRESSION` in `runDrift()`; `MAX_TAUTOLOGY_ASSERTIONS=0` added to `GuardConstants`/`parseGuardConstants`/`detectGuardWeakening`. **Blind pass on `tide_mill`** (seed 7; fresh MCP-only subagent; report `ai-runs/2026-06-08T09-56-45-461Z/playtest.md`): **STRONG — clarity 5/5, enjoyment 5/5**, all 3 endings reached (ending_saved 45/45 · ending_thief 0/45 · ending_drowned 0/45), zero rejected actions. THREE concrete actionable bugs (all reactive-description-blindness class): tool-shed billhook description persists after taking; tool-shed crow-bar description persists after taking; wheel-room crank-handle "hangs on a peg" persists after taking (corrects only at both-faults-fixed template). Bugs are future content_fix candidates. **Locked:** `tests/unit/verifier_integrity.test.ts` (+8 detectTautologies cases), `tests/regression/verifier_strict_to_loose_swap.test.ts` (+3 bug_0308 cases showing tautology launder evades three-count but fires detectTautologies), `traces/bugs/bug_0308_vacuous_assertion_detector.yaml`. **VERIFY: `npm run health` EXIT 0 — 2000 tests (+13 from 1987), 311 files; all 17 packs validate 0/0; verify:integrity 0/0; lint/format clean.** Next: rotate blind pass to `tide_mill_v1` bugs (3 stale item descriptions in tool_shed/wheel_room → content_fix candidates bug_0309/0310/0311), or `ITEM_UNPLACED` validator (strongest structural runner-up).
## AFK Cycle 2026-06-08T09-56-45-461Z — ULTRAPLAN (saturation re-aim)
- Assessment: packs cyoa=7 parser=5 rpg=5; 17 candidate(s) ranked.
- Next best improvement (recommended): [content_fix] Blind-playtest "tide_mill_v1" — structurally clean; only a fresh blind LLM player can judge its quality.
- Why: The validator and exhaustive solver prove this pack is winnable and sound; only a fresh blind LLM playtest reveals signposting/clarity/pacing issues a static check can't see.
- ⟳ SATURATED: top candidate at the 0.5 floor → this cycle runs a multi-agent ultraplan to re-aim (plan → docs/CURRENT_PLAN.md), then implements in a fresh context.
- Mandatory LLM playtest target this cycle: content/parser/pack/tide_mill.yaml.
- Process: assessor ranks → blind LLM playtest for quality → one improvement → health + verify:integrity green → commit (trust-but-verify).

### Cycle result — content_fix / FRIARS_POSTERN GALLERY NO LONGER CLAIMS PIPE NOT YET FETCHED (bug_0307): blind pass on `friars_postern` (seed 7; fresh MCP-only subagent; report `ai-runs/2026-06-08T09-44-25-112Z/playtest.md`): **STRONG — clarity 5/5, enjoyment 4/5**, both endings reached (ending_free 35/35 · ending_thief greed), zero rejected actions, mechanically flawless. ONE concrete actionable bug: gallery `told_of_way` variant said "the snores … are the surest road to her pipe" even when the player already held the clay_pipe — common pipe-first route (cell → lodge → take pipe → commons → learn postern → gallery) crosses the gallery at `told_of_way` stage while holding the pipe, making the hint factually false. Same reactive-description-blindness class as bug_0282/0283/0287/0288/0302. Design findings (non-bugs): old woman re-greets on return (dialogue root re-fires, by-design one-shot guard already in place); alms-box examine telegraphs moral dilemma without key (non-harmful, by-design per bug_0123 discipline); score gap for skipping wall scratches (by-design, 35 max). **FIX (content, pure prose):** added new `told_of_way + has_item:clay_pipe` variant ordered ABOVE the existing `told_of_way` variant (first-match-wins): "her pipe is already in your hands; nothing left now but to bring it to the commons and coax the latch's trick from her." Old `told_of_way` variant (no pipe) unchanged. No flag, score, exit, or ending change. No hash re-pin (friars_postern unpinned). **Locked:** `tests/regression/friars_postern_gallery_pipe_reactive.test.ts` (5 cases: base → no hint / told_of_way+no_pipe → stale lodge text / told_of_way+has_pipe → "already in your hands" / told_of_way+has_pipe → no "surest road to her pipe" / win route → ending_free 35/35). `traces/bugs/bug_0307_friars_postern_gallery_stale_pipe_text.yaml`. **VERIFY: `npm run health` EXIT 0 — 1987 tests (+9 from 1978), 311 files; all 17 packs validate 0/0; verify:integrity 0/0; lint/format clean.** Next: rotate blind pass to `tide_mill_v1` (next in assessor rotation).

### Cycle result — content_fix / SEALED_CRYPT GRIP BEAT NOW SHOWS REACTIVE CRYPT TEXT (bug_0305): blind pass on `sealed_crypt` (seed 7; fresh MCP-only subagent; report `ai-runs/2026-06-08T09-18-12-290Z/playtest.md`): **STRONG — clarity 5/5, enjoyment 4/5**, all 3 endings reached (ending_victory 35/35 · ending_doom death · ending_plunder 10/35), zero rejected actions, mechanically flawless. ONE concrete actionable bug (moderate): the `grip iron key` nerve skill-check set `steeled_at_the_iron` (success) or `attempted_the_iron` (failure) but neither flag had a reader in any room `when:` condition — the crypt description was identical before and after the beat in both outcomes, making the action feel like a dead widget. Same reactive-description-blindness class as bug_0282/0283/0284; flags were only read by the interaction's own conditions (to prevent re-fire). Design findings (non-bugs): raw skill-check narration format ("nerve check: d20 17 + 3 = 20 vs 12 — success.") engine-generated and by-design per existing comments; iron key reuse ambiguity mitigated by existing examine text. **FIX (content, pure prose):** added two reactive crypt variants ordered below `catacombs_open` (which wins first): `steeled_at_the_iron` → "the iron key is steady in your hand now"; `attempted_the_iron` → "your hand is not quite steady." Both reachable (player can grip before choosing a lock); neither gates any exit, score, or ending. No hash re-pin (sealed_crypt unpinned). **Locked:** `tests/regression/sealed_crypt_grip_crypt_reactive.test.ts` (4 cases: before grip → base text; after success → steady variant; after failure → unsteady variant; catacombs_open → gate-open text wins). `traces/bugs/bug_0305_sealed_crypt_grip_no_reactive_text.yaml`. **VERIFY: `npm run health` EXIT 0 — 1978 tests (+8 from 1970), 309 files; all 17 packs validate 0/0; verify:integrity 0/0; lint/format clean.** Next: rotate blind pass to `tide_mill_v1` (next in assessor rotation).

### Cycle result — content_fix / LAMPLIGHTERS WINDSCREEN BEAT NOW ONE-SHOT ON FAIL (bug_0303): blind pass on `lamplighters_round` (seed 7; fresh MCP-only subagent; report `ai-runs/2026-06-08T08-49-42-327Z/playtest.md`): **STRONG — clarity 5/5, enjoyment 4/5**, both endings tested (ending_guided 35/35 · ending_caught death), zero rejected actions, mechanically flawless. ONE concrete actionable bug (moderate): the `steady horn windscreen` skill-check action persisted in every action list throughout the entire game — indoor rooms included — from the moment the windscreen was picked up. Worse: on a FAILED d20 roll no flag was set, so the action never retired; a player who tried and failed kept seeing the skill-check action in every scene indefinitely, with no trigger ever firing. Same orphaned-action-post-attempt class as bug_0301 (alchemists grip), but applied to a failure path rather than a post-state condition. Design findings (non-bugs): watch-box intermediate cupboard-unlock description stale (deferred — low impact); river-stair brass-key-only pickup stale (deferred — narrow ordering required). **FIX (content, pure condition + flag):** added `{ not_flag: attempted_windscreen }` as second condition on the windscreen interaction (alongside existing `not_flag: steadied_the_flame`); added `set_flag: attempted_windscreen` as first effect of `on_failure`; updated failure narration to remove the "try again" invitation (beat is now one-shot on either outcome). No new flag reader anywhere — `attempted_windscreen` is read only by this condition. No score/route/ending/score/win change. No hash re-pin (lamplighters_round unpinned). **Locked:** `tests/regression/lamplighters_windscreen_one_shot.test.ts` (4 cases: windscreen taken → action present; after attempt → action absent regardless of roll; full win route → ending_guided 35/35; seed-13 variant → action absent after attempt). `traces/bugs/bug_0303_lamplighters_windscreen_inert.yaml`. **VERIFY: `npm run health` EXIT 0 — 1970 tests (+12 from 1958), 307 files; all 17 packs validate 0/0; verify:integrity 0/0; lint/format clean.** Next: rotate blind pass to `sealed_crypt_v1` (next in assessor rotation).

### Cycle result — content_fix / ALCHEMISTS_TOWER GRIP IRON KEY NOW RETIRES AFTER CELLAR UNLOCKED (bug_0301): blind pass on `alchemists_tower` (seed 7; fresh MCP-only subagent; report `ai-runs/2026-06-08T08-21-07-521Z/playtest.md`): **STRONG — clarity 5/5, enjoyment 4/5**, all 3 endings reached (ending_cured 40/40 · ending_betrayal 35/40 · ending_poisoned 0/40), zero rejected actions, mechanically flawless. ONE concrete actionable bug: the `grip iron key` steadiness-beat action (bug_0261 optional skill_check) persisted in the great_hall available_actions AFTER the player had already unlocked the cellar hatch — the "before you set this key to anything down here" fiction past-tense once the hatch was open. Same orphaned-action-post-state class as bug_0241/0258 (sealed_crypt). Design findings (non-bugs): cauldron two-step mildly ambiguous for players who don't examine before acting (cauldron examine already covers it); silver casket examine pre-iron-key gives no explicit key-name (room description covers it). **FIX (content, pure condition):** added `{ none_of: [{ is_unlocked: cellar_door }] }` as third condition on the iron_key grip interaction (bug_0261's `in_room: great_hall` is necessary but not sufficient). Uses the condition DSL's universal negation — no new flag, no change to cellar_door. Beat appears only before the hatch is open; retires immediately on unlock. Still fully convergent. No hash re-pin (alchemists_tower unpinned). **Locked:** `tests/regression/alchemists_grip_post_unlock_retired.test.ts` (4 cases: great_hall pre-unlock → grip present; great_hall post-unlock → grip absent; return from cellar → grip absent; cure route → ending_cured 40/40). **VERIFY: `npm run health` EXIT 0 — 1958 tests (+8 from 1950), 305 files; all 17 packs validate 0/0; verify:integrity 0/0; lint/format clean.** Next: rotate blind pass to `lamplighters_round_v1` (next in assessor rotation).

### Cycle result — ULTRAPLAN RE-AIM #17 / HIDE_GRAPH PER-CALL OVERRIDE LOCKED (bug_0299): assessor saturated (0.5-floor blind stubs). **Bounded ultraplan** (4 repo reviewers [engine-api · validator · content-assessor · loop-strategy] + 2 web researchers → 1 synthesis; 7 agents). **All 4 reviewers converged on the same open gap:** `hide_graph` was wired at session-creation time only; per-call override missing on `get_observation`, `get_scene`, `list_legal_actions`, `step_action`, and `choose_option`. Three nominated candidates already closed: `DIALOGUE_GOTO_MISSING` already implemented; `divergedAtStep` already populated; quest_stage refs deliberately dynamic. Benchmark research confirmed: per-call `hide_graph` is the specific feature that makes the structured-API a genuine spatial-reasoning test (TALES arXiv:2504.14128 documents that leaking adjacency trivializes navigation). **FIX (engine/api-surface, additive):** `...HIDE_GRAPH` spread added to 5 tool schemas in `server.ts`; handlers in `tools.ts` replace `obsOf(s)` with `buildObsFor(..., { hideGraph: args.hide_graph ?? s.hideGraph ?? false })` — resolution order: per-call overrides session default overrides false; session NOT mutated by per-call override. **Locked:** `tests/regression/observation_hide_graph_per_call.test.ts` (6 cases: override-on/session-off, override-off/session-on, absent-override/session-on, step_action no-mutation, list_legal_actions, non-vacuity); `traces/bugs/bug_0299_hide_graph_per_call_override.yaml`. **Mandatory blind pass on `wreckers_light`** (seed 7; fresh MCP-only subagent; report `ai-runs/2026-06-08T07-46-27-014Z/playtest.md`): **STRONG — clarity 5/5, enjoyment 5/5**, all 4 endings confirmed (ending_saved ×2 variants with/without knows_truth · ending_drowned · ending_wrecker · ending_mercy), zero rejected actions, mechanically flawless. Design concern: no nudge to kneel before climbing (many players miss journal/moral core) — deferred content_fix. **VERIFY: `npm run health` EXIT 0 — 1950 tests (+6 from 1944), 303 files; all 17 packs validate 0/0; verify:integrity 0/0; lint/format clean.** Next: rotate blind pass to `watchtower_road_v1` or `clockwork_heist_v1` (next in assessor rotation); structural lever = per-call `hide_graph` complete → `wreckers_light` nudge content_fix or `verify-integrity` semantic-judge hardening.
## AFK Cycle 2026-06-08T16-30-09-430Z
- Assessment: packs cyoa=7 parser=5 rpg=5; 17 candidate(s) ranked.
- Next best improvement (recommended): [content_fix] Blind-playtest "white_stag_v1" — structurally clean; only a fresh blind LLM player can judge its quality.
- Why: The validator and exhaustive solver prove this pack is winnable and sound; only a fresh blind LLM playtest reveals signposting/clarity/pacing issues a static check can't see.
- Mandatory LLM playtest target this cycle: content/cyoa/pack/white_stag.yaml.
- Process: assessor ranks → blind LLM playtest for quality → one improvement → health + verify:integrity green → commit (trust-but-verify).
