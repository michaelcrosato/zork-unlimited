# AI Loop State

### Cycle result — content_fix / CLOCKWORK DUMBWAITER NARRATION NO LONGER APOLOGETIC (bug_0289): blind pass on `clockwork_heist` (seed 13; fresh MCP-only subagent; report `ai-runs/2026-06-08T05-27-16-958Z/playtest.md`): **STRONG — clarity 5/5, enjoyment 4/5**, 4 playthroughs reaching all 4 endings (`ending_truth` both crawlspace 20/45 and vault-letter 35/45 · `ending_rich` 45/45 · `ending_patrol` dead), zero rejected actions, mechanically flawless. Playtester's "bugs" all non-bugs: score 35/45 on truth-via-vault was tester skipping the ledger read (not penalized morality — both letter and gold award +20; the 45 ceiling requires intel+feat+haul); ticks-10 deadline IS implemented (ending_overstayed); slip_out_quiet guile check resolves correctly (both branches → foyer). ONE concrete prose finding (Friction C): the dumbwaiter `narrate` effect closed "but it leaves you no closer to the vault than the open steps would" — a "but" conjunction that frames the option as a letdown. The no-bypass message is already conveyed by "the very floor the staircase serves" (same narration); the apologetic qualifier adds no information. Same family as bug_0046 (which over-corrected from stealth-bypass overclaim to apologetic disclaimer). **FIX (content, pure):** drop the final clause; new close is "A quieter way up from the kitchens, and a more direct one." Honest (destination still named), positive (no apology), no state/routing/tick/flag/ending change. Updated `tests/regression/clockwork_dumbwaiter_honest_framing.test.ts` assertion from `"no closer to the vault"` → `"very floor the staircase serves"` (same semantic guarantee, new anchor). No hash re-pin (clockwork_heist unpinned). **VERIFY: `npm run health` EXIT 0 — 1899 tests (0 net change — updated existing test), 294 files; all 17 packs validate clean 0/0; verify:integrity 0/0; lint/format clean.** Next suggested focus: rotate blind pass OFF clockwork_heist → `dead_reckoning_v1` or `midnight_edition_v1` (next in assessor rotation).

### Cycle result — content_fix / CLOCKWORK VAULT TICKS>=2 NO LONGER PRESUPPOSES READ_LEDGER (bug_0288): blind pass on `clockwork_heist` (seed 42; fresh MCP-only subagent; report `ai-runs/2026-06-08T05-13-39-593Z/playtest.md`): **STRONG — clarity 5/5, enjoyment 5/5**, 4 playthroughs reaching all 3 endings (`ending_truth` both vault and crawlspace variants · `ending_rich` 45/45 · `ending_caught` dead), zero rejected actions, mechanically flawless. Playtester flagged one concrete bug: vault `ticks>=2` variant said "you know what the hour wakes: fill your hands and be gone before the chime sets the watchman walking" even without `read_ledger` set. A no-ledger player reaches the vault at ticks 2-3 via kitchen→dumbwaiter→approach_vault (which gates on `ticks<=3 OR read_ledger`); the "you know" claim and "the watchman" reference presuppose ledger knowledge they never earned. Same reactive-description-blindness class as bug_0282/0283/0284/0286/0287. Note: a `ticks>=2 AND read_ledger` companion would be a dead variant — earning the ledger requires visiting study (+1 tick) and returning through landing (+1 tick), giving minimum 4 ticks at vault entry for any ledger-reading path; the `ticks>=4` variant (implicitly ledger-gated) already handles that case correctly. **FIX (content, pure):** vault `ticks>=2` variant drops "you know what the hour wakes:" and "the watchman" (both unearned without the ledger), replacing with state-neutral "fill your hands and be gone before the chime sets the manor stirring." No choice/flag/effect/item/exit/gating/ending change — prose only. No hash re-pin (clockwork_heist unpinned). **Locked:** `tests/regression/clockwork_vault_ticks_ledger_reactive.test.ts` (4 cases: ticks=2 no-ledger → "manor stirring" / no "you know" / no "watchman" · ticks≥4 ledger → "ledger swears" / no "manor stirring" · choices grab_gold+take_letter present · all 5 endings reachable). **VERIFY: `npm run health` EXIT 0 — 1899 tests (+4 from 1895), 294 files; all 17 packs validate clean 0/0; verify:integrity 0/0; lint/format clean.** Next suggested focus: rotate blind pass to `midnight_edition_v1` or `watchtower_road_v1` (next in assessor rotation).

### Cycle result — content_fix / CLOCKWORK STUDY NO LONGER PROMPTS TO READ LEDGER ALREADY READ (bug_0287): blind pass on `clockwork_heist` (seed 42; fresh MCP-only subagent; report `ai-runs/2026-06-08T04-52-05-586Z/playtest.md`): **STRONG — clarity 5/5, enjoyment 4/5**, 4 playthroughs reaching all 3 endings (`ending_truth` both crawlspace and vault variants · `ending_rich` 45/45 · `ending_caught` dead), zero rejected actions, mechanically flawless. Playtester flagged ticks mechanic as "hollow" (never fired in their runs — by design, fires only at ticks≥10 for aimless wandering, max honest play ≈6). Post-playtest structural review found reactive-description-blindness in the study: ticks≥2 variant closed "take what this page can tell you and be gone" and ticks≥7 closed "no still room left in which to read a stolen page" — both presuppose the ledger is unread, but both variants fire when `read_ledger` IS set (a player circling upstairs after reading). Same class as bug_0282/0283/0284/0286. ticks≥4 variant ("watch has begun to walk") is state-neutral (describes observable facts) and needs no companion. **FIX (content, pure):** study gains `ticks≥7 AND has_flag:read_ledger` variant ("its last page already turned; you have what the schedule could give you … nothing left in this still room for you") and `ticks≥2 AND has_flag:read_ledger` variant ("the schedule it kept is yours now … you know what the chime wakes"), each placed ABOVE its plain-ticks counterpart (first-match-wins). No choice/flag/effect/item/exit/gating/ending change — prose only. No hash re-pin (clockwork_heist unpinned). **Locked:** `tests/regression/clockwork_study_read_ledger_reactive.test.ts` (7 cases: ticks≥2 unread → stale prompt / ticks≥2 read → new text / ticks≥7 unread → "stolen page" / ticks≥7 read → new text / ticks 4-6 with read_ledger → plain ticks≥4 fires / choices gated correctly / all 5 endings reachable). **VERIFY: `npm run health` EXIT 0 — 1895 tests (+16 from 1879), 293 files; all 17 packs validate clean 0/0; verify:integrity 0/0; lint/format clean.** Next suggested focus: rotate blind pass OFF clockwork_heist → `midnight_edition_v1` (next in assessor rotation) or `lamplighters_round_v1`/`friars_postern_v1`.

### Cycle result — content_fix / MIDNIGHT_EDITION ALLEY DOOR NAMES GARROW BEFORE LETTER READ FIXED (bug_0284): blind pass on `midnight_edition` (seed 42; fresh MCP-only subagent; report `ai-runs/2026-06-08T04-18-18-429Z/playtest.md`): **STRONG — clarity 5/5, enjoyment 4/5**, 4 playthroughs reaching all 4 endings (`ending_vindicated` 35/35 · `ending_silenced` · `ending_spiked` · `ending_libel` 5/35), zero rejected actions, mechanically flawless. One concrete actionable bug: `alley_door` base text "Garrow's yard-men, and not a printer's tool among the hammers…" rendered even when the player had not yet read the letter — the quest_stage variants (story_in_hand / proof_sought / proof_found) all correctly name Garrow because they fire only post-read_letter, but the base fires when no stage is set, leaking the antagonist's identity before any in-game introduction. Same reactive-description-blindness class as bug_0120/0134/0282/0283. **FIX (content, pure):** base text changed "Garrow's yard-men" → "Hired men" — same menace, neutral attribution; all four quest_stage variants and the door_barred variant unchanged. Cosmetic only — no flag/choice/route/ending/score change. No hash re-pin (midnight_edition carries no pinned hash). **Locked:** `tests/regression/midnight_edition_alley_garrow_attribution.test.ts` (4 cases: base → "hired men" / no "garrow's" · story_in_hand → "garrow's" present · door_barred variant wins · choices unchanged + vindicated route 35/35 reachable). **VERIFY: `npm run health` EXIT 0 — 1879 tests (+4 from 1875), 290 files; all 17 packs validate clean 0/0; verify:integrity 0/0; lint/format clean.** Next suggested focus: rotate blind pass OFF midnight_edition → `watchtower_road_v1` (next in assessor rotation) OR `breaking_weir_v1`/`friars_postern_v1` (deferred).

### Cycle result — content_fix / WATCHTOWER TOWER_TOP BRAZIER FALSE AFFORDANCE FIXED (bug_0283): blind pass on `watchtower_road` (seed 42; fresh MCP-only subagent; report `ai-runs/2026-06-08T04-04-29-865Z/playtest.md`): **STRONG — clarity 5/5, enjoyment 4/5**, 3 playthroughs reaching `ending_truth` (50/50), `ending_captured` (0/50), `ending_escape` (non-fatal), zero rejected actions, mechanically flawless. One concrete actionable bug: `tower_top` base text "A cold brazier waits, begging for a flame" rendered even when the player had no lantern — `light_beacon` correctly gated on `has_item: lantern`, so the scene described an affordance the player couldn't access. Same reactive-description-blindness class as bug_0120 (tower_top post-lighting stale text), bug_0134 (hermit_talk), bug_0282 (mossy_brook). **FIX (content, pure):** `tower_top` gains a `not_item: lantern` variant ordered between `raised_alarm` and the base — neutral "dark and empty" form when lantern absent; base "begging for a flame" only shows to a lantern-holder pre-lighting. Cosmetic only — no flag/choice/route/ending/score change. **Locked:** `tests/regression/watchtower_tower_top_brazier_affordance.test.ts` (4 cases: no-lantern → neutral text / with-lantern → invite text / raised_alarm → blazing variant / choices unchanged + ending_escape reachable). **VERIFY: `npm run health` EXIT 0 — 1875 tests (+4 from 1871), 289 files; all 17 packs validate clean 0/0; verify:integrity 0/0; lint/format clean. Deliberate hash re-pin: watchtower_road 24aaf2b9… → 9fed3843… (surfaced, not weakened).** Next suggested focus: rotate blind pass OFF watchtower_road → `friars_postern` gate examine reactive text (deferred bug: gate text says "The scratches were plain about this one" even when `read_clue` not set — reactive-description-blindness class, single-scene content fix) OR `breaking_weir_v1` (next in assessor rotation).

### Cycle result — content_fix / WATCHTOWER MOSSY_BROOK STALE CAMP TEXT FIXED (bug_0282): blind pass on `watchtower_road` (seed 7; fresh MCP-only subagent; report `ai-runs/2026-06-08T03-49-19-615Z/playtest.md`): **STRONG — clarity 4/5, enjoyment 4/5**, 3 playthroughs reaching `ending_truth` (50/50), `ending_captured` (5/50), `ending_escape` (15/50), zero rejected actions, mechanically flawless. One concrete bug: `mossy_brook` kept discovery-framing "a thread of smoke marks a small camp downstream" after the player visited the hermit, heard the tale (`heard_hermit_lore`), or had the letter read (`seal_broken`) — same reactive-description-blindness class as bug_0134 (hermit_talk re-greeting) and bug_0120 (tower_top brazier). **FIX (content, pure):** `mossy_brook` gains a variant gated on `any_of: [heard_hermit_lore, seal_broken]` (the same guard as the bug_0134 fix in hermit_talk) — the post-visit form reads "the hermit's camp" instead of "a small camp"; base text unchanged for first-time visitors. Cosmetic only — no flag/choice/route/ending/score change. **Locked:** `tests/regression/watchtower_mossy_brook_stale_camp.test.ts` (4 cases: base shows on first visit / lore arm fires / seal arm fires / choices unchanged and ending_truth reachable). **VERIFY: `npm run health` EXIT 0 — 1871 tests (+4 from 1867), 288 files; all 17 packs validate clean 0/0; verify:integrity 0/0; lint/format clean. Deliberate hash re-pin: watchtower_road 89a0c84a… → 24aaf2b9… (surfaced, not weakened).** Next suggested focus: rotate blind pass OFF watchtower_road → `friars_postern` gate examine reactive text (deferred from bug_0281 cycle: gate text says "The scratches were plain about this one" even when read_clue not set — reactive-description-blindness class, single-scene content fix) OR `breaking_weir_v1` (next in assessor rotation).

### Cycle result — ULTRAPLAN RE-AIM #15 / VALIDATOR NOW CATCHES DANGLING ITEM REFS (bug_0281): assessor saturated (all 17 packs blind-clean; 0.5-floor stubs). **ULTRAPLAN re-aim cycle** — bounded workflow (4 repo reviewers [engine/validator · content/authoring · verification/benchmark · loop/strategy] + 2 web researchers [IF/agentic benchmarks · reward-hacking] → 1 synthesis; 7 agents, ~207k tokens). **3 of 4 repo reviewers converged on the same move**, confirmed open by the orchestrator. **RE-AIM: `ITEM_REF_MISSING`** — seals the final intra-frame reference-integrity rung: bug_0277 (room refs in conditions/effects) + bug_0278 (unlock_exit room refs) + bug_0281 (item refs in add_item/remove_item) = all three reference families sealed. Gap confirmed: `allEffects(pack)` at `src/validate/parser_validator.ts:486/491` collects `add_item`/`remove_item` targets but never cross-checks them against `objById`; a typo'd `add_item: "phantom"` silently inserts an uninteractable phantom item into inventory, never flagged by `ITEM_REQUIRED_UNOBTAINABLE` (which checks liveness, not existence). Collateral: two inline test packs in `parser_score_peaks_before_win.test.ts` used `add_item: relic` with no declared object — the new check correctly caught them at bail-early, requiring `relic` to be declared in both packs (legitimate latent defects, now fixed). **VERIFY: `npm run health` EXIT 0 — 1867 tests (+11 from 1856), 287 files; all 17 packs validate clean 0/0; verify:integrity 0/0; lint/format clean; no hash re-pin (no pack content changed).** **Mandated blind pass on `friars_postern`** (PARSER; seeds 7/13/3; fresh MCP-only subagent; report `ai-runs/2026-06-08T03-23-06-889Z/playtest.md`): **STRONG — clarity 4/5, enjoyment 4/5**, all 3 endings reached (`ending_free` 35/35 · `ending_taken` 0/35 · `ending_thief` 5/35), zero rejected actions, mechanically flawless. Two content findings: (1) gate examine text says "The scratches in your cell were plain about this one" even when `read_clue` is NOT set (reactive-description-blindness class, sibling of the bug_0279 ending fix — deferred content_fix); (2) `use_gate_key` skill-check action floats in available_actions in every room where key-ring is held, not just the gatehouse (orphaned USE class — deferred). **Next suggested focus:** the three-family reference-integrity seal (bug_0277+0278+0281) is now the formal precondition for the world-frame manifest schema (multi-cycle); nearest single-cycle structural moves: (1) benchmark scorecard module (`src/afk/benchmark.ts` + `bin/benchmark.ts` + freshness pin test — M effort); (2) friars_postern gate examine reactive text (deferred content_fix from this playtest); (3) assessor above-floor category (blocked on API key).

### Cycle result — content_fix / FRIARS_POSTERN GATE ENDING NO LONGER PRESUMES THE SCRATCHES (bug_0279): blind pass on friars_postern (seed 3, fresh MCP-only subagent; report `ai-runs/2026-06-08T01-41-59-556Z/playtest.md`): **STRONG — clarity 5/5, enjoyment 4/5**, all 3 endings reached across 3 runs, zero rejected actions, mechanically flawless. One concrete bug surfaced: `ending_taken` closed "You trusted the gate **the scratches told you not to trust**" — but Playthrough 2 went straight to the lodge and tried the gate without ever reading the wall-scratches (`read_clue` unset). The ending accused the player of ignoring advice they never received — the **reactive-ending-blindness class** (bug_0272/0276). **FIX (content_fix, pure reactive-prose — bug_0279):** `ending_taken` gains a `variants: [{ when: [{ has_flag: read_clue }], text: ... }]` block above its base text (first-match-wins). The base text drops the scratches reference ("You trusted the gate — and the dark beyond the bars was full of waiting men…"); the `read_clue` variant keeps the ironic callback for a player who DID read the warning and trusted the gate anyway. Both routes genuinely reachable (straight-to-gate never sets `read_clue`; read-then-gate does) → base is no dead fallback; both land on `ending_taken` → cosmetic reframe, never a reroute. No flag/score/exit/win/death/schema change. **Locked:** `tests/regression/friars_postern_gate_reactive_epilogue.test.ts` (5 cases: unread path reaches `ending_taken` with `read_clue` unset; read path reaches with it set; unread → base text, NOT the scratches callback; read → variant text WITH callback; both land on same ending id with distinct rendered text). **VERIFY: `taskset -c 0-3 npm run health` EXIT 0 — 1856 tests (+5 from 1851), 285 files; all 17 packs validate clean 0/0; verify:integrity 0/0; lint/format clean; no hash re-pin (friars_postern carries no pinned hash).** Next suggested focus: rotate blind pass OFF friars_postern → `breaking_weir_v1` (next overdue per assessor list). The reactive-ending-blindness class is now closed on SIX packs (tithe_barn bug_0272, wreckers_light bug_0273, cold_forge bug_0275, barrow bug_0276, friars_postern thief bug_0186, friars_postern gate bug_0279).

### Cycle result — validator / UNLOCK_EXIT ROOM REFS NOW VALIDATED (bug_0278) + blind playtest friars_postern STRONG: ULTRAPLAN re-aim cycle #14 — chosen structural lever: validateParser was missing `unlock_exit.from`/`.to` in its room-id integrity pass. bug_0277 sealed conditions (`visited`/`not_visited`/`in_room`) + `goto`/`place_object.room` effects; bug_0278 seals the final gap: a typo'd `unlock_exit.from` or `.to` silently writes `__exit:phantom->real` — the unlock APPEARS to fire (no exception) but the exit flag never matches (engine keys on declared room ids), a permanent no-op no oracle caught. **FIX (validator, additive):** dedicated `UNLOCK_EXIT_ROOM_MISSING` emit block placed after `UNRESOLVED_ROOM_REFERENCE` in `validateParser`, scanning `allEffects(pack)` for `unlock_exit` effects and checking both sides against `roomIds`; added to bail-early guard alongside `EXIT_TARGET_MISSING`/`START_MISSING` (a dangling room id corrupts the settable-flags set used by graph analysis). New broken fixture `content/broken-fixtures/parser_unlock_exit_room_missing.yaml`, registered in `VALIDATOR_FIXTURES`; `UNLOCK_EXIT_ROOM_MISSING` case in negative corpus; dedicated `tests/regression/parser_unlock_exit_room_missing.test.ts` (5 cases: shipped packs clean · dangling `from` · dangling `to` · non-vacuity · both sides dangling → two findings); two `traces/bugs/bug_0278_*.yaml` artifacts. Bug_0277+0278 together seal every room-id-bearing construct in the pack schema — the formal precondition for the deferred world-frame manifest is now met. **VERIFY: `taskset -c 0-3 npm run health` EXIT 0 — 1851 tests (+10 from 1841); all 17 packs clean with ZERO `UNLOCK_EXIT_ROOM_MISSING` (5 unlock_exit uses across tide_mill/lamplighters_round/sealed_crypt, all valid); verify:integrity 0/0; lint/format clean.** **Mandated blind pass on `friars_postern`** (PARSER; seeds 7/13/3; fresh MCP-only subagent; report `ai-runs/2026-06-08T01-10-02-147Z/playtest.md`): **STRONG — clarity 5/5, enjoyment 5/5**, all 3 endings found (`ending_free` 35/35 · `ending_taken` 0/35 · `ending_thief` 30/35), zero rejected actions, mechanically flawless; `knows_postern` gate on chapel north verified (no 4th ending); skill check legibility + quest stage tracking + score economy all correct. Four minor friction findings (none mechanical bugs): chapel room pre-narrates a stone-press the player never explicitly took; heft failure path unseen; ending_thief fires in-place though postern is open; old woman's precise instructions set up a puzzle that auto-resolves via flag. **Next suggested focus:** deferred structural rungs — (1) world-frame manifest schema (bug_0277+0278 are its preconditions, now met; multi-cycle), (2) BFS AG(EF goal) forward-reachability validator (blast-radius L, health time-budget risk, deferred), (3) Goal-2 dev/blind split (independent track). Blind rotation: next overdue = breaking_weir / dawn_beacon / watchtower_road per assessor list.

### Cycle result — ULTRAPLAN RE-AIM #13 / VALIDATOR NOW CATCHES DANGLING ROOM REFERENCES (bug_0277): assessor saturated as ever (all 17 packs blind-clean; ranked list = 0.5-floor blind stubs). This was an **ULTRAPLAN re-aim cycle** — the prior `docs/CURRENT_PLAN.md` (cycle #12, bug_0262) was 14 cycles stale, so a bounded local ultraplan (Workflow tool: **4 repo reviewers** [engine/determinism · content/authoring+generators · verification/benchmark · loop/strategy] **+ 2 web researchers** [frontier IF/agentic benchmarks · verification-at-scale/reward-hacking] **→ 1 synthesis**; 7 agents, 144 tool-uses, ~331k subagent tokens) re-aimed the project, grounded against HEAD = bug_0276 and advancing `docs/ULTRAPLAN-2026-06-02.md` + `docs/ROADMAP.md`. **ALL FOUR repo reviewers independently converged on the same pick**, then the orchestrator re-verified its openness in source before committing. **RE-AIM CHOSEN: `UNRESOLVED_ROOM_REFERENCE`** — the verbatim still-open cycle-#12 deferred reference-integrity lever (the cycle-#12 move INERT_OBJECT_STATE bug_0262 + its deferred safety sibling WIN_FIRES_AT_START bug_0270 BOTH landed since, sealing object-state on feasibility+liveness+relock+win-stability; this is the next genuinely-open rung on the assume-guarantee ladder toward the deferred world-frame manifest). **THE GAP (orchestrator-verified in source):** `roomIds` (`src/validate/parser_validator.ts:110`) was consulted ONLY at `start_room` (:131), `exit.to` (:150), `npc.room` (:210) — NEVER on a `visited`/`not_visited`/`in_room` condition's room id (bare `z.string().min(1)`, `src/core/conditions.ts:20-29`) nor on `goto`/`place_object.room` effect targets (`src/core/effects.ts:29/40`). A typo'd room id silently evaluates **false forever** (`conditions.ts:80-82`) — a permanently-dead gate that NO existing oracle catches: the static SOFTLOCK pass reads it as a deliberate stable-false gate, the exhaustive-BFS solver as an unreachable atom, the metamorphic relabel oracle is bijectively blind (twin keeps the same typo). **FIX (validator, additive — bug_0277):** new error code `UNRESOLVED_ROOM_REFERENCE` via a fresh `collectRoomRefs(pack)` walker placed after `collectObjectStateReads` (~:1564), mirroring `collectFlagReads` EXACTLY (descends all_of/any_of/none_of over the same sites — room/object/win/ending-variant/dialogue) + `goto`/`place_object.room` from `allEffects`; emits `err()` (default **error** severity, like EXIT_TARGET_MISSING) for any ref not in `roomIds`. RPG covered FREE via `validateParser` delegation (`rpg_validator.ts:102`) — no RPG edit. **PROVABLY GREEN-PRESERVING:** a standalone YAML scan over all shipped parser+RPG packs found **14 room refs, 0 dangling** (the two "apparent" danglers in breaking_weir/wolf_winter are inside YAML `#` comments — never parse), so the code fires ZERO times on shipped content. No schema/engine/effects/conditions/generators/rpg_validator/pack-content/hash/scorecard/corpus-seal/verify-integrity change; relabel oracles need no edit (room ids already pass through the bijection). **Locked:** `tests/regression/parser_unresolved_room_reference.test.ts` (cases a–d: all shipped parser+RPG packs zero findings; `visited`+`in_room` conditions fire at `severity==="error"`; `goto`+`place_object.room` effects fire; **mandatory non-vacuity** — correcting the ids to a declared room clears it) + broken fixture `content/broken-fixtures/parser_unresolved_room_reference.yaml` registered in `tests/unit/parser_validator.test.ts` VALIDATOR_FIXTURES + one error-severity case in `tests/regression/parser_validator_negative_corpus.test.ts` + `traces/bugs/bug_0277_parser_unresolved_room_reference.yaml` (SoundnessBench lineage, arXiv:2412.03154). **VERIFY (orchestrator-run): `taskset -c 0-3 npm run health` EXIT 0 — 1841 tests / 282 files pass (up from 1833, strengthening the TEST_COUNT guard), all 17 packs validate clean with ZERO `UNRESOLVED_ROOM_REFERENCE`, verify:integrity 0/0 (no GUARD_WEAKENED/VERIFIER_TOUCHED), dedicated test 6/6, lint/format clean.** No pack added/removed ⇒ bug_0194 scorecard freshness pin unaffected (no regen). **Mandated blind pass ran on `alchemists_tower`** (PARSER, rank-2 longest-overdue; seeds 7/13/3; fresh MCP-only general-purpose subagent, no content/src/ui/tests access; report `ai-runs/2026-06-05T07-29-21-465Z/playtest.md`): **STRONG — clarity 5/5, enjoyment 4/5**, all three declared endings reached (`ending_cured` 40/40 win · `ending_poisoned` death · `ending_betrayal` 30/40), **mechanically flawless** (zero rejected actions, zero broken state, no loops; reactive prose + lock/key + score economy verified), **ZERO mechanical bugs**. Sole §5 note (recorded, NOT this cycle's structural move): the `grip iron key` `steadiness` check is now legible (bug_0274) but mechanically **INERT** — cellar unlocks without it, the flag has no observed downstream effect, the action lingers after useful; the recurring vestigial-self-USE skill_check family (see [[affordance-signpost-class]]), a candidate content/quest fix for a FUTURE cycle (telegraph-then-room-gate, or give the check a real consequence). **Next suggested focus / re-aim deferrals (full detail in `docs/CURRENT_PLAN.md` cycle #13):** the heavier open-world rungs are sequenced AFTER this one — (1) promote the test-only forward-reachability/no-dead-pocket BFS (`support/exhaustive_endings.ts`, bug_0150) into a validator-integrated AG(EF goal) pass (blast-radius L, risks the health time budget, largely duplicative — deferred), (2) the world-frame manifest schema + modular cross-region static reachability (multi-cycle; THIS move is its precondition), (3) the Goal-2 dev/blind-test loop split (process/infra, independent track). Rotate the mandated blind pass OFF alchemists_tower → `friars_postern_v1` (other longest-overdue parser) or an RPG. Lever remains ENGINE/validator/content DEPTH on existing packs (no new packs); the keyed real-model authoring run is still the only out-of-reach keystone (needs API key).

### Cycle result — content / THE SUNKEN BARROW'S DOOM EPILOGUE NO LONGER PRESUMES THE SHADE (bug_0276): assessor saturated as ever (all 17 packs blind-clean; ranked list = 0.5-floor blind stubs, rank-1 nominee `sunken_barrow_v1`, the harness's mandated target this cycle). **Mandated blind pass ran on `sunken_barrow`** (RPG, the rank-1 nominee; seeds 11 & 5; fresh MCP-only general-purpose subagent, no content/src/ui/tests access; report `ai-runs/2026-06-05T07-11-45-654Z/playtest.md`): came back **STRONG — clarity 5/5, enjoyment 4/5**, BOTH endings reached ("Lord of the Barrow" victory 50/50 seed 11; "What Sleeps Beneath" doom-death 25/50 seed 5), **mechanically flawless** (zero rejected actions, zero broken state, no loops; the +3 ward, retryable might check, combat teeth, reactive room/object prose all verified), **ZERO mechanical bugs**. Its strongest §5 finding: on the seed-5 run it reached `ending_woken` (prise the sarcophagus) having **SKIPPED the shade entirely** (straight north to the wight, never west to the cell), yet the doom epilogue closed "and the shade's last counsel rings true after all" — a non-sequitur, since it presumes the shade's warning was heard but the doom fork is reachable with `heard_lord_lore` UNSET (the shade is optional, bug_0075). This is the **[[reactive-ending-blindness-class]]** (bug_0271/0272/0273/0275): a terminal reachable on BOTH an informed and an uninformed path whose single fixed text presumes the informed one — the EXACT class flagged for an audit sweep last cycle, and now the sibling the very next blind pass surfaced on the original barrow. Until last cycle this was unfixable on RPG (ParserEndingSchema had no `variants`); **bug_0275 built that surface and first spent it on cold_forge — this cycle spends it again on the defect the rotation found.** **FIX (content_fix, pure reactive-prose — bug_0276):** `ending_woken` gains a `variants: [{ when: [{ has_flag: heard_lord_lore }], text: ... }]` ABOVE the base (first-match-wins). The base text now de-presumes the shade ("and some instinct older than reason tells you the land was the better for his sleeping" — reads true for a player who never met it); the `heard_lord_lore` variant keeps the original callback ("the shade's last counsel rings true after all") for the player who DID hear the warning and prised the coffin anyway. Both paths genuinely reachable (under-armed straight-to-fight never sets the flag; warded route does) → base is no dead fallback; both land on the SAME `ending_woken` id → cosmetic reframe, never a reroute. **No flag/score/quest/exit/win-condition/reachable-ending/schema change.** `ending_woken` is not on the canonical victory trace, so that trace's state-based `expected_final_hash` is UNCHANGED; only the pack `content_hash` shifted (one deliberate, surfaced re-pin in `traces/rpg/barrow_victory.json`: 32c55798… → d2343930…). **Locked:** `tests/regression/rpg_barrow_woken_reactive_epilogue.test.ts` (3 cases against the real RPG runner + observation: informed route → reframed callback in `ending.text` + `description`; uninformed route → base text and NOT the callback, pinning the non-sequitur gone; both = death endings at 25/50) + `traces/bugs/bug_0276_barrow_woken_reactive_epilogue.yaml`. The auto-discovered RPG variant-liveness proof now auto-declares the new variant and proves it first-match in a reachable terminal (best/worst-roll bracket) — a dead variant fails LOUD there too. **VERIFY: `npm run health` EXIT 0 under `taskset -c 0-3` — 1833 tests / 281 files pass, all 17 packs validate clean, 0 integrity/lint/format errors.** No pack added/removed ⇒ bug_0194 scorecard freshness pin unaffected (no regen). **Next suggested focus:** the reactive-ending-blindness class is now closed on FOUR packs across all devices (tithe_barn fall bug_0272, dead_reckoning hub bug_0271, wreckers drowned bug_0273, cold_forge bug_0275, barrow woken bug_0276) — continue the AUDIT SWEEP flagged in [[reactive-ending-blindness-class]]: every pack with a "truth"/knowledge flag, check each non-gated terminal reachable on both informed and uninformed paths for a presumptive epilogue. Rotate the mandated blind pass OFF sunken_barrow → rank-2 `alchemists_tower_v1` (longest-overdue parser) or `friars_postern_v1`. Lever remains ENGINE/content DEPTH on existing packs (no new packs); the keyed real-model authoring run is still the only out-of-reach keystone (needs API key).

### Cycle result — engine / PARSER+RPG ENDINGS NOW CARRY REACTIVE EPILOGUES (bug_0275): assessor saturated as ever (all 17 packs blind-clean; ranked list = 0.5-floor blind stubs, rank-1 nominee `cold_forge_v1`, the harness's mandated target this cycle). **Mandated blind pass ran on `cold_forge`** (RPG, the rank-1 nominee; seeds 17 & 3; fresh MCP-only general-purpose subagent, no content/src/ui/tests access; report `ai-runs/2026-06-05T06-39-54-147Z/playtest.md`): came back **STRONG — clarity 5/5, enjoyment 4/5**, BOTH divergent routes reached "Keeper of the Ember" 50/50 across 2 playthroughs, **mechanically flawless** (zero rejected actions, zero broken state, no loops; reactive room/object prose, no-pry-bar redirect, retryable might check, combat teeth all verified), **ZERO bugs**. Its one substantive §5 design note: "a thorough player gets no payoff for exploring the dialogue and the west cell" — hearing the lantern-spirit's tale of the forge's last master, finding his cell, reading his epitaph, then carrying out his Ember-Heart ALL ended on the SAME epilogue as a rusher who skipped every word of it. The pack's whole optional second branch — and its emotional point, that you complete the dead master's last work ("Carry the Heart out, and the forge is not wholly dead") — was invisible at the one moment it should land. **ROOT = a capability gap, not a defect:** CYOA endings have carried reactive `variants` since bug_0145, but ParserEndingSchema (shared by parser AND RPG) had none — which is exactly why every recent reactive-ending fix (bug_0271/0272/0273) was CYOA-only. With the no-new-packs directive (2026-06-04) pointing the lever at ENGINE depth, this is the highest-value fix: build the surface and spend it. **FIX (engine + content — bug_0275):** ported reactive ending variants to parser/RPG across all touch points — schema (`ParserEndingVariantSchema` + `variants` optional on `ParserEndingSchema`, mirroring RoomVariantSchema; absent ⇒ byte-identical compile, all hashes hold), model (`endingText` first-match resolver), observation (resolve once for `description` + structured `ending.text`; RPG inherits free), validator (ending-variant UNREACHABLE_VARIANT/UNSATISFIABLE_CONDITION guards + flag/object-state read walks), the relabel metamorphic oracle (relabels ending-variant `when`s), and BOTH variant-liveness proofs (credit + declare ending variants at terminal states). Then spent it on cold_forge: both endings get a `knows_founder` variant (any_of[visited: founder_cell, has_flag: heard_founder]) — ending_victory names the master + the legacy carried out, ending_fallen reframes to a death that joins him in the cold; honors the reactive-ending-blindness class (BOTH non-gated terminals, each reachable informed AND uninformed, reframe). Prose-only: no score/flag/quest/exit change — prep stays about survival, not points. **This COMPLETES the reactive-prose trilogy across all 3 modes** (rooms/objects/dialogue/endings now reactive in parser+RPG; CYOA already had scenes+endings). **Verify:** typecheck clean; validate(cold_forge) 0 findings; new lock test `cold_forge_reactive_epilogue.test.ts` 8/8 GREEN (5 epilogue routes win/death × known/unknown founder + heard_founder-only leg + 3 validator negative controls); parser/rpg/cyoa variant-liveness 25 GREEN; relabel + cold_forge suites GREEN; **`npm run health` exit 0** (one prior run flaked on a CPU-contention 60s-timeout sweep of 19 RPG exhaustive suites incl. `rpg_score_economy_sound` which my change doesn't touch — re-ran clean; isolated heavy-RPG run 25 GREEN in 15.7s). No pack added/removed ⇒ bug_0194 scorecard freshness pin unaffected; no hash re-pin. **Next suggested focus:** rotate the mandated blind pass OFF cold_forge → `sunken_barrow_v1` (the OTHER RPG, the original; now that parser/RPG endings can reframe, a barrow pass may surface a similar knowledge/prep epilogue-flatness worth the new device) or `alchemists_tower_v1` (rank-3, longest-overdue parser). Lever remains ENGINE/content DEPTH on existing packs (no new packs); the keyed real-model authoring run is still the only out-of-reach keystone (needs API key).

### Cycle result — engine / SKILL-CHECKED PARSER+RPG USES NOW SURFACE THEIR STAT + DIFFICULTY (bug_0274): assessor saturated as ever (all 17 packs blind-clean; ranked list = 0.5-floor blind stubs, rank-1 nominee `sealed_crypt_v1`, the harness's mandated target this cycle). **Mandated blind pass ran on `sealed_crypt`** (PARSER, the rank-1 nominee; seed 13; fresh MCP-only general-purpose subagent, no content/src/ui/tests access; report `ai-runs/2026-06-05T06-17-21-081Z/playtest.md`): came back **STRONG — clarity 5/5, enjoyment 4/5**, ALL THREE forks reached across 3 playthroughs (victory 35/35 / plunder / doom-death), **mechanically flawless** (zero rejected actions, zero broken state, no loops; reactive prose + lock/key + score economy all verified), **ZERO bugs**. The two §4/§5 findings shared ONE root: the parser observation never told the player that the crypt's optional `grip iron key` self-USE is a **nerve check (d20 + nerve vs DC 12)** — so the `nerve` stat shown in `state.vars` every turn "reads like a stat with no mechanical role" and the action "reads oddly … a flavor/no-op self-use." `nerve` IS the var that beat rolls (a convergent tension beat, bug_0241/0258), but `available_actions` exposed it as a bare `{ id, command, action }`, identical to a plain command. This is the PARSER/RPG sibling of the CYOA gap **bug_0269** closed last cycle (skill-checked CYOA choices now surface stat + difficulty). With the blind pass clean and the no-new-packs directive (2026-06-04) pointing the lever at ENGINE depth, this is the highest-value fix. **FIX (engine_rule, observation-only — bug_0274):** `enumerateActions` now attaches `skill_check: { skill, difficulty }` to a skill-checked USE option (`ParserActionOption.skill_check`); `buildParserObservation` AND — for free, via the shared `enumerateRpgActions` wrapper — `buildRpgObservation` surface it. Only `skill`/`difficulty` are exposed, NEVER the check's `on_success`/`on_failure` effects (which carry score/flag/end_game routing), so the destination graph stays hidden by construction (exactly as a plain command never exposes its effects). OMITTED on every non-skill action (plain commands + ATTACK) ⇒ legacy observation shape byte-identical for every existing pack. The web UI renders a "⟨nerve check, DC 12⟩" label suffix (ui/src/engine.ts, parser + rpg branches). Both metamorphic per-step observation oracles (parser + rpg) strengthened in lock-step: the surfaced skill name is an IDENTIFIER (an author var), so it maps through the relabel bijection — keeping the contamination-robustness witness sound and broader. This CLOSES the skill_check-legibility class across all THREE modes (CYOA bug_0269 + parser/RPG bug_0274). NOT a benchmark leak: a printed DC reveals no destination room or effect (cf. hide_graph bug_0137). Locked by `tests/regression/sealed_crypt_skill_check_legibility.test.ts` (5 cases) + `traces/bugs/bug_0274_parser_skill_check_legibility.yaml`. **VERIFY: `npm run health` EXIT 0 — 1822 tests pass (279 files), all 17 packs validate clean, 0 lint/format/integrity errors.** NEXT SUGGESTED FOCUS: rotate the mandated blind pass OFF sealed_crypt (played this cycle) → rank-2 `cold_forge` (RPG) is due, and an RPG pass would now also exercise the NEW skill_check-surface on an RPG skill-checked USE (wolf_winter `defense`, breaking_weir `nerve`) in the wild — good cross-check that the shared-enumeration plumbing reads right to a fresh player.

### Cycle result — content / WRECKERS_LIGHT'S INFORMED DROWNING NOW REFRAMES (bug_0273): assessor saturated as ever (all 17 packs blind-clean; ranked list = 0.5-floor blind stubs, rank-1 nominee `wreckers_light_v1`, the harness's mandated target this cycle). **Mandated blind pass ran on `wreckers_light`** (CYOA, the rank-1 nominee; seed 3; fresh MCP-only general-purpose subagent, no content/src/ui/tests access; report `ai-runs/2026-06-05T06-04-01-004Z/playtest.md`): came back **STRONG and CLEAN — clarity 5/5, enjoyment 5/5**, ALL four narrative endings reached (saved/wrecker/mercy) plus the `ending_drowned` death across three playthroughs + a direct mercy branch, **mechanically flawless** (zero rejected actions, no loops, no broken state; quest stages, reactive Gallery/Lantern-Room/mantel prose, and the knows_truth epilogue reframes all verified), **ZERO bugs**. With the blind pass clean and the no-new-packs directive (2026-06-04) pointing the lever at content DEPTH, the cycle closed a **reactive-epilogue symmetry gap** the pass passes over: the pack's whole "truth" device reframes the SAME act under `knows_truth` (saved → "...KNOWING whose hand is at the wheel"; wrecker → "...you tell yourself it was justice"; mercy → "...he sees that you already know"), but `ending_drowned` — climb over the gallery rail into the sea — was the ONE non-gated ending that did NOT reframe. It is reachable on BOTH the ignorant climb (`enter → out_gallery → climb_to_wreck`) AND the KNOWING one (read the journal first, return to the hub, climb anyway), yet only the ignorant text existed: a player who went over the rail having just read the Mourning Star carries the man who drowned the keeper's son read the same death as one who never learned. This is the EXACT class of bug_0272 (tithe_barn's `ending_fall`) and bug_0271 (dead_reckoning's deck hub) — the [[reactive-ending-blindness-class]] applied to the rank-1 focus pack. **FIX (content_fix, pure reactive-prose — bug_0273):** added a `variants: [{ when: [{ has_flag: knows_truth }], text: ... }]` block ABOVE `ending_drowned`'s base text (first-match-wins): the informed climb now renders the reframed death (you go over the rail KNOWING whose bell tolls, the lamp stays unlit, the Mourning Star drowns with you — "the rocks do not trouble to tell them apart"), the ignorant climb still renders the base. Both paths genuinely reachable → base is no dead fallback; both land on the SAME `ending_drowned` id → cosmetic reframe, never a reroute, the same shape as saved/wrecker/mercy. **No flag/route/choice/quest_stage/gate/scoring/schema change; no new scene or ending; the death stays equally telegraphed.** **Locked:** `traces/bugs/bug_0273_wreckers_informed_drowned_reframes.yaml` + `tests/regression/wreckers_informed_drowned_reframes.test.ts` (4 cases: informed reframe live + supersedes base, uninformed base reached with no dead fallback, both climbs converge on `ending_drowned`, non-vacuity). **TEETH verified out of band:** neutering the new variant's guard (`knows_truth` → `never_set_sentinel`, never set) turned exactly 2 of the 4 RED (informed-reframe + non-vacuity), the two base cases stayed GREEN; restoring it returned 4/4 GREEN. Markers are layer-unique single lines ("you go down knowing whose bell still tolls" variant-only; "the white water is not cold for long" base-only). **Evidence:** `npm run health` GREEN end-to-end (verify:integrity 0/0, typecheck, lint, format, **1817 tests / 278 files pass**, all 17 packs validate 0/0; wreckers_light prose changed but discovered-pack set unchanged → bug_0194 scorecard freshness pin stays green, no regen). No src/validator/engine/schema/generator/scorecard/corpus byte changed → the wreckers_light branching + signpost (bug_0257) suites stay GREEN. **Next suggested focus:** rotate the mandated blind pass OFF wreckers_light (played this cycle) onto the next least-recently-played CYOA/pack — `sealed_crypt` or `cold_forge` (ranks 2-3). The reactive-ending-blindness class is now closed on THREE packs across two devices (tithe_barn fall bug_0272, dead_reckoning hub bug_0271, wreckers drowned bug_0273) — worth an audit SWEEP of every pack with a "truth"/knowledge flag for non-gated terminals reachable on both informed and uninformed paths that don't reframe (the systematic sibling of the affordance-signpost sweep flagged in [[affordance-signpost-class]]).

### Cycle result — content / WOLF_WINTER'S OPTIONAL WEDGE IS NOW SIGNPOSTED + DISCOVERABLE (bug_0258): assessor saturated as ever (all 17 packs blind-clean; ranked list = 0.5-floor blind stubs, rank-1 nominee `wolf_winter_v1`, the harness's mandated target this cycle). **Mandated blind pass ran on `wolf_winter`** (RPG, the rank-1 nominee; seed 11; fresh MCP-only general-purpose subagent, no content/src/ui/tests access; report `ai-runs/2026-06-05T05-48-14-545Z/playtest.md`): came back **STRONG — clarity 5/5, enjoyment 4/5**, two routes (prepared + reckless rush) both reaching `ending_held` (60/60 prepared, 45/60 reckless), **mechanically flawless** (zero rejected actions, no loops, no broken state; combat math, quest stages, blocked-exit messages, and the bug_0256 reactive prose all verified). It surfaced **ONE design flaw**: the fallen `paling_rail` reads as a vestigial dead clue — its examine text promises a "wedge it across the gap" mechanic the tester concluded "does not exist anywhere." **TRIAGE (reproduced via MCP):** the literal claim is factually WRONG — the pack DOES carry the wedge (its only skill_check, self-USE `command_verb: wedge`, defense vs 11, + the bug_0256 reactive payoff). But the USE/wedge action only surfaces with the rail **IN HAND** (RPG runner self-USE rule), so a player who fights the obvious wolf first never takes the rail and never sees it — a true **DISCOVERABILITY** flaw, the sequential sibling of bug_0256 (which made the wedge legible AFTER you do it; nothing made it discoverable). The pack's only skill_check + all its bug_0256 prose was dead content for a real first-time player. Compounding it, the paling_gap reactive line "the way old Cade swore the byre would funnel them" attributed to Cade a promise he never actually made about the rail. **FIX (content_fix, pure prose — bug_0258):** old Cade's "how the byre is held" counsel (cade_byre — the natural info path the player walks BEFORE the paling) now SIGNPOSTS the take-then-wedge ("...one stout rail's fallen clear into the snow there. Take it up and wedge it back across the gap, stamp it home, and you'll half-shut the breach — what comes through after must come singly and slow..."), with a matching persistent heard_plan journal reminder. The affordance is now discoverable on the natural path AND the paling_gap callback to Cade is now literally true. PURE prose: cade_byre's effects stay exactly {set_flag heard_plan, add_journal} — no new flag/condition/effect/route/ending — and the wedge stays an OPTIONAL, stat-neutral aid gating no exit/win/ending, so the bug_0114 fairness guarantee, the bug_0189 three-fight cumulative-gauntlet tuning, and the bug_0239 prep-weighted score economy are all BYTE-UNCHANGED (those regressions pass untouched). **Locked:** `traces/bugs/bug_0258_wolf_winter_wedge_signpost.yaml` + `tests/regression/wolf_winter_wedge_signposted.test.ts` (5 cases: signpost text, prose-only effects, runner-surfaced spoken+journal signpost, the wedge action genuinely surfaces after take, and breach_braced gates no exit/win/ending). **Evidence:** `npm run health` GREEN end-to-end (verify:integrity 0/0, typecheck, lint, format, **1813 tests / 277 files pass**, all 17 packs validate 0/0; wolf_winter content_hash now `b94a6718…`, not pinned by any test — see [[content-polish-backlog-exhausted]] bug_0195). This is a new leg of the [[reactive-ending-blindness-class]] / affordance-signpost family (cf. bug_0257 wreckers gallery): the deterministic solver proves an optional affordance is SOUND and the pack winnable, but only a blind LLM playtest catches whether a real player ever DISCOVERS a take-gated optional aid. **Next suggested focus:** rotate the mandated blind pass OFF wolf_winter (played this cycle) onto the next least-recently-played RPG — `sunken_barrow` or `breaking_weir`; watch specifically for the same discoverability class on other optional, take-gated or condition-gated affordances (the affordance-signpost family now has two confirmed legs across modes — wreckers CYOA bug_0257, wolf_winter RPG bug_0258 — so it's worth an audit sweep of optional self-USE skill_checks across the other RPG/parser packs for take-gated invisibility).

### Cycle result — content / TITHE_BARN'S DEATH ENDING NOW REFRAMES UNDER knows_truth (bug_0272): assessor still saturated (all 17 packs blind-clean; ranked list = 0.5-floor blind stubs, rank-1 nominee `tithe_barn` — the exact pack the prior cycle's Next-focus named). **Mandated blind pass ran on `tithe_barn`** (CYOA, the harness's rank-1 nominee; seed 13; fresh MCP-only general-purpose subagent, no content/src/ui/tests access; report `ai-runs/2026-06-05T05-33-21-886Z/playtest.md`): came back **STRONG and CLEAN — clarity 5/5, enjoyment 4/5**, four runs reaching **all five endings** (`ending_doors` "The Town at the Grain", `ending_bounty` "The Lord's Silver", `ending_fall` "The Rotten Loft" death, `ending_mercy` "The One Sack" + the gating verified both with and without `knows_truth`), **mechanically flawless** (zero rejected actions, no loops, no stale/contradictory text), the `open_doors` knows_truth gate and the reactive granary_floor/reckoning prose all verified, **NO bug**. The only §4 friction notes were genre-intentional (the two-step book read; the `back_to_floor` no-op return). With the content verified clean, this cycle closed a reactive-DEPTH gap the pass could pass over (same class as last cycle's [[content-polish-backlog-exhausted]] hub-staleness work): tithe_barn's "truth" device reframes the SAME act in the epilogue (bounty → "...you do it KNOWING"; mercy → "you saved the one you could see"; doors needs no variant — GATED on knows_truth so always the knowing act), but **`ending_fall` — the death — was the one NON-gated ending that did NOT reframe**, though it has both an ignorant climb (`go_in → climb_loft`) AND a knowing one (`go_in → read_book → decipher → leave_book → climb_loft`). The death read identically whether or not you had just read that the grain about to bury you was a deliberate hoard. **FIX (content_fix, pure):** added a `knows_truth` `variants:` block to ending_fall (above the base `text`), first-match-wins — the informed climb now renders the reframed death (you read the loft holds the oldest of a hoard left to rot, climbed after more of it for yourself alone, and that same hoard "buries the one man who read it" while the lie goes on; telegraphed exactly as the base). Both climbs stay genuinely reachable, so the base is no dead fallback and both land on the SAME `ending_fall` id — the identical cosmetic-reframe-never-reroute shape as bounty/mercy. No flag/route/choice/quest_stage/gate/scoring/schema change; no new scene/ending; doors stays variant-free. **TEETH:** new `tests/regression/tithe_barn_informed_fall_reframes.test.ts` (4 cases on the REAL buildObservation surface: informed climb → "you set a boot to the ladder knowing" & "the one man who read it" & NOT the base; uninformed climb → the base line & NOT the knowing line; both converge on `ending_fall`; the two are distinct renders). Out-of-band non-vacuity confirmed: neutering the variant guard turned 2/4 RED, restored to 4/4 with only the intended +27-line pack addition. Locked by `traces/bugs/bug_0272_tithe_barn_informed_fall_reframes.yaml`. **No hash re-pin / no scorecard regen** (no pack added/removed → discovered set unchanged; existing tithe_barn_branching bug_0136 + quest_stage_reactive_prose bug_0255 suites stay green — the new variant touches none of their markers); **no schema/engine/validator/runner/determinism change** — content only. **Bars:** `validate_story(tithe_barn)` → ok, 0 findings; `npm run health` **EXIT 0** under `taskset -c 0-3` (net +4 test cases; verify:integrity 0/0; typecheck + lint + format clean; all 17 packs validate clean, tithe_barn OK 0/0). Diff scope = `content/cyoa/pack/tithe_barn.yaml` (ending_fall variant + design note) + new regression test + new bug artifact + this loop-state entry (additive; no src/schema/validator/generator/hash/scorecard/corpus change — determinism untouched). **Pattern note:** when a pack's "truth" flag reframes its epilogues, audit EVERY non-gated ending that has both an informed and an uninformed path — a death/terminal the player reaches once reads fine in isolation but is the likeliest to stay knowledge-blind (cf. the hub-staleness class: bug_0264/0268/0271; this is its ENDING-side sibling). **Next suggested focus:** all 17 packs remain blind-clean; rotate the NEXT mandated blind pass OFF tithe_barn onto the next-overdue 0.5-floor nominee — `wolf_winter` / `wreckers_light` / `sealed_crypt` per the assessor list. Structural/soundness/reactive-prose oracle nets stay saturated (don't re-attempt the closed families). Genuine off-deterministic-assessor levers remain: the keyed real-model author→play→fix→lock run ([[ultraplan-true-goal-pivot]], owner-API-key-gated) and the open-world / dev-vs-blind-test loop split ([[next-phase-roadmap]]).
## AFK Cycle 2026-06-08T05-27-16-958Z
- Assessment: packs cyoa=7 parser=5 rpg=5; 17 candidate(s) ranked.
- Next best improvement (recommended): [content_fix] Blind-playtest "clockwork_heist_v1" — structurally clean; only a fresh blind LLM player can judge its quality.
- Why: The validator and exhaustive solver prove this pack is winnable and sound; only a fresh blind LLM playtest reveals signposting/clarity/pacing issues a static check can't see.
- Mandatory LLM playtest target this cycle: content/cyoa/pack/clockwork_heist.yaml.
- Process: assessor ranks → blind LLM playtest for quality → one improvement → health + verify:integrity green → commit (trust-but-verify).
