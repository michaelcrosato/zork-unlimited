{
  "schema_version": 1,
  "id": "c19692972f585a85",
  "title": "Verify Grok CLI headless contract and register it as a dev-loop driver",
  "body": "Verify the Grok CLI against the dev-loop vendor contract and, if it conforms,\nregister it in `loop.sh`'s dev-agent registry (the `DEV_AGENT_IDS` list and its\ndetect/invocation entries, ~3 lines) so `AI_AGENT=grok ./loop.sh` works like\ncodex/claude/gemini.\n\nThe contract (loop.sh vendor section): read the cycle prompt from STDIN, edit\nfiles in `$PWD`, run non-interactively, exit nonzero on failure. To verify:\nrun the CLI headless with a trivial prompt in a scratch directory and confirm\nall four properties, then record the exact flags used in the registry entry.\n\nContext: a `grok.exe` process was observed running on the dev machine\n(2026-08-30, the #306 playtest wave driver spawns `grok` directly), but the\nCLI did not resolve on the verifying session's PATH, so the flags could not be\nverified then — do not add the registry entry on faith. Until this lands, a\nGrok dev lane runs via `AI_AGENT_CMD=\"grok <verified-flags>\" ./loop.sh`\n(documented in docs/parallel_lanes.md).\n\nWhile in there, note the codex-only `AI_CODEX_SANDBOX` knob: decide whether an\nequivalent is needed per vendor or whether the generic contract suffices.",
  "source": "human",
  "kind": "feature",
  "priority": "P2",
  "status": "open",
  "labels": [
    "lane:ops"
  ],
  "area": null,
  "evidence": {
    "summary": "Filed by human.",
    "refs": [],
    "lineages": [],
    "observations": 1
  },
  "created_at": "2026-08-30T21:54:38.536Z",
  "updated_at": "2026-08-30T21:54:38.536Z",
  "external": null
}
