id: bug_0324
pack: dawn_beacon_v1
severity: minor
class: reactive-description-blindness
found_by: blind_playtest
found_cycle: 2026-06-08T13-56-45-884Z
seed: 7

summary: >
  beacon_crown room description still mentioned "the watch's own flint lies in the bed
  beside it" after the player had picked up the flint — the visible_objects list
  updated correctly but the prose did not. Same reactive-description-blindness class
  as bugs 0282–0323.

scene: beacon_crown
repro:
  - Navigate the full prepared route to beacon_crown (cresset righted).
  - TAKE flint.
  - Re-read the room description — it still says "the watch's own flint lies in the bed
    beside it" even though the flint is now in the player's hand.

fix:
  type: content_prose
  change: >
    Added a `variants` block to the beacon_crown room. When `has_item: flint` is true
    (player holds the flint) the variant fires: fires "The flint is in your hand — one
    struck spark to wake the valley." instead of "the watch's own flint lies in the bed
    beside it." Base description (flint in room, not yet taken) is unchanged.
    No flag, score, exit, or ending change; prose only.

regression_test: tests/regression/dawn_beacon_crown_flint_reactive.test.ts
