bug_id: bug_0024
pack_id: alchemists_tower_v1
seed: 29                          # the regression's deterministic seed (43 used for the mandated clockwork playtest)
type: content_fix                # pure content; applies the existing bug_0023 object-`variants` engine feature
evidence:
  source: blind_playtest + standing_backlog
  protocol: docs/blind_playtest_protocol.md
  mandated_report: ai-runs/2026-06-01T09-01-56-782Z/playtest.md   # clockwork_heist seed 43 — the cycle's mandated target
  mandated_result: >
    EIGHTH straight pristine clockwork run: a fresh MCP-only playtester reached all four
    endings (Truth via vault AND via the crawlspace strongbox, Caught, Caught on the
    Hour) + the gold/greed branch, rated clarity 5/5 / enjoyment 4/5, hit zero rejected
    actions, no loops, no broken state, and confirmed the bug_0019 ledger-gate crossing
    is mechanically correct. Its only two notes were NOT on-screen defects: (a) the
    ending_truth prose presumes the vault route even when reached via the strongbox (a
    known cosmetic item logged since bug_0019/bug_0023); (b) the strongbox shortcut skips
    the deadline tension with no tradeoff (a balance wrinkle, not a bug). Clockwork is
    polished past diminishing returns (bug_0008/0017/0018/0019/0020/0022/0023 + this run).
  rotation_rationale: >
    Per the protocol's "you MAY pick a different candidate if your judgement and the
    playtest say it's higher value", this cycle closes bug_0023 deferred[0] — applying
    the now-available object-`variants` feature to alchemists_tower's strongbox — which
    fixes a REAL on-screen contradiction, higher value than a 9th clockwork micro-note.
  finding: >
    bug_0012 made the Cluttered Study ROOM react (after the iron key is held it reads
    "The iron strongbox sits open and empty ... its brass lock sprung"), and bug_0023
    shipped reactive OBJECT descriptions (ObjectSchema.variants + objectDescription, via
    the single LOOK render site) — but explicitly DEFERRED applying it here. So the
    strongbox OBJECT still examined as the static "A squat iron strongbox with a brass
    lock" after being unlocked, opened, and emptied. CONFIRMED LIVE via MCP (sess_3):
    navigated to the study, unlocked + opened the box, took nothing, then `look at iron
    strongbox` → "A squat iron strongbox with a brass lock." (the bug) — a direct
    contradiction of the room's own reactive prose and the player's own actions.
improvement:
  layer: content                 # pure content; no engine/validator/schema change
  content:
    - content/parser/pack/alchemists_tower.yaml: >
        strongbox gains a `variants` entry gated on `has_item: iron_key` — the SAME
        durable "unlocked and emptied" proxy the study room uses (the closed condition
        DSL has no object-open predicate, src/core/conditions.ts), so the examine flips
        in lockstep with the room rather than introducing a new lag. New text: "The
        squat iron strongbox sits open and empty, its brass lock sprung and its lid
        thrown back — nothing left inside but the faint smell of old iron." Examine-only:
        no flag / item / score / exit / interaction / gating / reachable-ending change.
  engine:
    - none: >
        The generic reactive-object feature already exists (bug_0023). This pack simply
        opted in. No src/ change, no validator change.
verification:
  validate: green (0 errors, 0 warnings) — alchemists_tower under the full parser validator
  content_hash: 04d4e8be75dec47d8b5a92a36d2606b6b0458df116bf8a9ee2d5f8dcc99dcd1e   # was 8302bfdfd0e9ee57426d278730bf9782989120de1396a8b6d313ee71a802cb60; surfaced, not laundered. Unpinned (only watchtower_road is pinned in tests).
  health: exit 0 (verify:integrity OK, format:check + lint + all tests + every pack validate + watchtower coverage)
  reachable_endings_unchanged: [ending_cured, ending_poisoned]   # examine-only change; canonical brew route still reaches ending_cured at 35/35
verified_live: >
  Through MCP (new_game seed 43, step_action): examining the strongbox after unlock+open
  returned the static "A squat iron strongbox with a brass lock." (the bug). After the
  fix, exercised through the engine render site the regression drives (resolveParserAction
  LOOK→strongbox via initStateForParserPack / enumerateActions / makeStep — the same path
  the MCP server uses): the examine reads the base box before the iron key is held (even
  after unlock+open), flips to "open and empty / brass lock sprung" once the key is taken,
  and never again reads "with a brass lock"; the canonical brew route still wins at 35/35.
regression_test: tests/regression/alchemists_strongbox_reactive_examine.test.ts   # 3 cases: (1) examine flips locked→open-and-empty in lockstep with the iron-key proxy (live LOOK render path); (2) a variant-less object (spellbook) → base text under arbitrary states + variants undefined (backward-compat); (3) reachability — canonical brew route still reaches ending_cured at full score.
closes_deferred:
  - bug_0023 deferred[0] "Apply the new object `variants` to alchemists_tower's strongbox" — done.
deferred:
  - The repo/ESLint + Prettier tooling gap (lint is still just tsc — the assessor's standing rank-3 repo candidate).
  - clockwork ending_truth prose presumes the vault route even when reached via the crawlspace strongbox (cosmetic continuity; logged across bug_0019/bug_0023 and this cycle's playtest §5).
