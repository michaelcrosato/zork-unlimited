Before any words or other action, call
`mcp__adventureforge__start_overworld({})` exactly once and never call it again.
Then call only preloaded
`mcp__adventureforge__*` gameplay functions authorized by the latest game
result. Never use any other tool. Make the next game choice only after the
current function result is visible.
