# MUST FAIL: SCHEMA
# An empty room description violates min(1) (§7) — rejected at the load boundary,
# before the validator ever runs.
meta: { id: bad_empty, title: "Bad Empty", start_room: a, vars_init: { hp: 10, attack: 2, defense: 1 } }
rooms:
  - id: a
    name: "A"
    description: ""
    exits: [{ direction: north, to: b }]
  - id: b
    name: "B"
    description: "Room B."
    exits: [{ direction: south, to: a }]
win_conditions: [{ id: w, conditions: [{ visited: b }], ending: e }]
endings: [{ id: e, title: "End", text: "Done." }]
