/**
 * Exact authored preparation copy from the manifest immediately before the
 * Station comparison gained trigger categories and Jamie's Relief Protocol
 * named its complete failed-wedge, spent-guard, return-to-Cade prerequisite.
 */
export const RELIEF_PROTOCOL_TRIGGER_COPY_PREDECESSOR_SUMMARY =
  "Carry Jamie Tanner's witnessed cadence for settling the herd after a rough rail recovery.";
export const RELIEF_PROTOCOL_TRIGGER_COPY_PREDECESSOR_PREVIEW =
  "After an ordinary split-rail recovery redirects the yearling alive, this opens one Mediation check (DC 12) before the loft cast. Success coordinates Cade's count and lowers cattle alarm by 1. Failure contradicts the yard calls and raises alarm by 1; the protocol retires without blocking the lure route. Mediation training improves the single attempt.";
