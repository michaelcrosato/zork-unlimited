# External-review completion record

This record maps the independent review of commits `5bb7947b` and `43d69416`
to the repository state prepared for the 2026-08-07 closeout. It is an evidence
index, not a substitute for the verification bar.

Status terms used below:

- **Implemented** means the named implementation and regression evidence exist
  in the current change.
- **Decision complete** means the owner-level policy is recorded and enforced
  where the repository can enforce it.
- **External execution pending** means the repository cannot complete the item
  without an owner-selected external service or credential.
- Every implementation status remains subject to the final checklist at the end
  of this document. A proof command is named as the authoritative check; this
  document does not imply that every command has already passed.

The architectural rationale is append-only in
[`DECISION_LOG.md`](./DECISION_LOG.md). Owner choices and continuing boundaries
are in
[`EXTERNAL_REVIEW_PROCESS_DECISIONS.md`](./EXTERNAL_REVIEW_PROCESS_DECISIONS.md).

Verifier retirement marker: `D10 save-migration ladder deletion`.

## Defect register

### New findings (`N1`–`N7`)

| Finding                                                                            | Status          | Implementation evidence                                                                                                                                                                                                                                                                                                   | Regression evidence / proof command                                                                                                                                                                       |
| ---------------------------------------------------------------------------------- | --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `N1` — numeric-gate feasibility, phantom variables, and unreachable `in_room` wins | **Implemented** | `src/validate/rpg_foundation_validator.ts` computes conservative reachable numeric ranges, emits `PHANTOM_VAR`, and includes `in_room` in win-room analysis. Negative fixtures live under `content/broken-fixtures/foundation_*.yaml`.                                                                                    | `tests/regression/rpg_numeric_gate_feasibility.test.ts`; `tests/regression/rpg_foundation_negative_corpus.test.ts`; `npm run validate`                                                                    |
| `N2` — TALK chose an absent same-named NPC                                         | **Implemented** | `src/rpg/command_map.ts` resolves only state-visible NPCs and fails closed on co-present ambiguity. `src/rpg/schema.ts` and `src/rpg/model.ts` provide state-reactive NPC presentation. Wolf-Winter now uses one stable `june_pike` identity in `content/rpg/quests/wolf_winter.yaml`; `AMBIGUOUS_NPC_NAME` is an error.  | `tests/regression/rpg_talk_visible_npc_resolution.test.ts`; `tests/regression/rpg_variant_liveness.test.ts`; `tests/regression/rpg_foundation_negative_corpus.test.ts`; `npm run validate -- wolf_winter` |
| `N3` — compact direct-road costs used a multi-hop route                            | **Implemented** | `src/world/compact_view.ts` derives each `roads` row from the direct edge while keeping multi-hop plans in `route_options`.                                                                                                                                                                                               | `tests/regression/compact_road_cost_truth.test.ts`; `npm run crawl:smoke`                                                                                                                                 |
| `N4` — crawler RENDER oracle covered too little visible text                       | **Implemented** | `src/crawl/oracles.ts` walks blocked exits/actions, visible names, pressure text, action commands, and skill-check stakes from a full observation.                                                                                                                                                                        | `tests/acceptance/crawler_fault_injection.test.ts`; `npm run crawl:smoke`                                                                                                                                 |
| `N5` — quest duration depended on the renown reward                                | **Implemented** | `src/world/session_quests.ts` separates `QUEST_COMPLETION_LOCAL_WORK_MINUTES` from `QUEST_COMPLETION_RENOWN`.                                                                                                                                                                                                             | `tests/unit/world_session_quests.test.ts`; `tests/unit/mcp_tools.test.ts`; `tests/unit/ui_overworld.test.ts`                                                                                              |
| `N6` — Wolf-Winter prose lived in an engine overlay                                | **Implemented** | Dialogue-node and NPC `variants[]` are authored schema features in `src/rpg/schema.ts`; resolution is content-agnostic in `src/rpg/model.ts`. The former engine dialogue overlay is deleted and the player prose is in `content/rpg/quests/wolf_winter.yaml`, inside the content hash.                                    | `tests/regression/dialogue_reactive_npc_text.test.ts`; `tests/regression/rpg_metamorphic_relabel.test.ts`; `tests/regression/wolf_winter_dialogue_surface.test.ts`; `npm run validate -- wolf_winter`     |
| `N7` — reveal legality was not snapshot-derived                                    | **Implemented** | `src/world/session.ts` is the single reveal/inspection/choice authority. `inspectedStoryReveals` is built, cloned, strictly restored, and validated through `src/world/session_snapshot.ts`, `src/world/session_snapshot_builder.ts`, and `src/world/session_persistence.ts`. MCP, terminal, and UI call the session API. | `tests/starting_slice/opening_dispatch_briefing.test.ts`; `tests/unit/mcp_journey_surface.test.ts`; `tests/unit/ui_story_choice_summary_first.test.ts`; `tests/unit/world_session_snapshot.test.ts`       |

### Earlier findings (`D1`–`D10`)

| Finding                                                              | Status                                  | Implementation evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Regression evidence / proof command                                                                                                                                                                                                                                                                                   |
| -------------------------------------------------------------------- | --------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `D1` — MCP error amplification and catch-path failure                | **Implemented**                         | `src/mcp/server.ts` guards error-message access, caps emitted error text, and bounds opaque snapshot/save payloads and patch arrays before deep parsing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `tests/regression/mcp_pure_play_mode.test.ts` (malformed and oversized restore/load cases)                                                                                                                                                                                                                            |
| `D2` — ineffective integrity floors and unwired drift ratchet        | **Implemented**                         | `scripts/verify-integrity.ts` has corpus-scale floors, protects the solver/backstop, and fails when base evidence cannot be read. `.github/workflows/ci.yml` uses full history and runs drift against the PR base.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `tests/unit/verifier_integrity.test.ts`; `tests/unit/ci_test_groups.test.ts`; `npm run verify:integrity`; `npm run verify:integrity -- --against <base-ref>`                                                                                                                                                          |
| `D3` — accepted seed bits were discarded                             | **Implemented**                         | `src/core/rng.ts` preserves the historical unsigned-32-bit stream and uses a deterministic 64-bit path for signed/wide safe-integer seeds. Save versions make the continuation boundary explicit.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `tests/property/determinism.test.ts`; `tests/regression/rng_known_answer_vector.test.ts`; `tests/unit/save_trace.test.ts`                                                                                                                                                                                             |
| `D4` — crawl smoke did not block the required check                  | **Implemented**                         | `.github/workflows/ci.yml` includes `crawl-smoke` in `verify.needs` and requires its success explicitly.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `tests/unit/ci_test_groups.test.ts`; `npm run crawl:smoke`                                                                                                                                                                                                                                                            |
| `D5` — stale PID reuse could kill an unrelated process               | **Implemented**                         | `loop.sh`, `scripts/loop-stop.sh`, and `scripts/loop-status.sh` record and authenticate Linux PID plus `/proc/<pid>/stat` start ticks; `INT`/`TERM` clean records; stop revalidates every target before signaling.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `tests/regression/loop_process_scripts.test.ts`                                                                                                                                                                                                                                                                       |
| `D6` — UI was outside the Prettier gate                              | **Implemented**                         | `ui` is an explicit target of `format`/`format:check` in `package.json` and is not ignored by `.prettierignore`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `npm run format:check`                                                                                                                                                                                                                                                                                                |
| `D7` — event decorators could mutate the live event array            | **Implemented**                         | `src/core/engine.ts` gives `decorateEvents` a detached snapshot and does not append that snapshot when returned unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | `tests/unit/engine.test.ts`                                                                                                                                                                                                                                                                                           |
| `D8` — saves lacked a state digest and player-stat ceilings          | **Implemented**                         | RPG save v3 in `src/persist/save_load.ts` uses strict versioned envelopes and canonical `stateHash`; `src/rpg/state_integrity.ts` enforces conservative authored `hp`/`attack`/`defense` ceilings. The digest is consistency evidence, not authentication.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | `tests/unit/save_trace.test.ts`; `tests/regression/save_integrity_adversarial.test.ts`; `tests/regression/save_load_referential_integrity.test.ts`                                                                                                                                                                    |
| `D9` — dead `contents`, dead `unlock_exit`, misleading `is_unlocked` | **Implemented**                         | Current `ObjectRuntime` has no `contents`; save v1 alone has a narrow compatibility parser. `unlock_exit` is retired. The condition is named `is_explicitly_unlocked` and its runtime-only meaning is documented.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `tests/unit/save_trace.test.ts`; `tests/unit/effects.test.ts`; `tests/unit/conditions.test.ts`; `tests/regression/save_load_referential_integrity.test.ts`                                                                                                                                                            |
| `D10` — compounding content-hash migration ladder                    | **Implemented; focused proof observed** | `src/world/session_snapshot.ts` defines structural world schema v11 with v10 as the only predecessor envelope. Its upgrader accepts current-ID/current-boundary v10 subshapes, strips old hashes as non-authoritative, and fails closed on hash-authorized legacy markers or proofs that lack current causal evidence. `src/world/session_snapshot_restore.ts` validates current state instead of branching on historical content hashes. A world-content mismatch restores against current content and says its prior journal is preserved while current authored content governs future play; world identity and all causal/integrity proofs remain strict. Historical copy shims, predecessor fixtures, and migration-only suites are deleted. | `tests/unit/world_session_snapshot.test.ts`; `tests/unit/world_session_snapshot_restore.test.ts`; `tests/starting_slice/albany_station_return_filing_standard_counterfactual.test.ts`; `tests/unit/ui_overworld.test.ts`; `tests/unit/verifier_integrity.test.ts`; `npm run verify:integrity -- --against <base-ref>` |

## Phase 0 owner decisions

| Review decision                          | Disposition                                    | Repository authority and remaining boundary                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ---------------------------------------- | ---------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Add an opening-density counter-metric    | **GO; implemented**                            | `scripts/verify-opening-density.ts` measures the real compact start and fails above 732 word tokens or 12 immediately actionable options. It is wired through `package.json`, health, CI, and `.github/workflows/deep-audit.yml`; `tests/unit/opening_density.test.ts` and `tests/unit/process_audit_wiring.test.ts` pin the contract.                                                                                                                                                                                                              |
| Run a cross-family blind tester          | **GO; backend implemented, execution pending** | The bounded experiment is specified in `EXTERNAL_REVIEW_PROCESS_DECISIONS.md`: at least 10 fresh pure journeys, same clean revision and neutral contract, separate from retention authority. `bin/playtest-grok-wave.ts` now supplies the non-Codex Grok Build lane, `src/qa/save_playtest_report.ts` binds completed reports to private V2 server evidence, and `blind-tester/prompt-grok-mcp-instant.md` carries the neutral player contract. The client remains `operator_attested`, and credentials plus live spend remain owner/external work. |
| Add a non-player evidence channel        | **GO; implemented**                            | `.github/workflows/deep-audit.yml` runs the deep crawl plus bug-trace, density, ending-proof, and standard-coverage audits nightly and on manual dispatch. `scripts/verify-bug-traces.ts`, `scripts/verify-opening-density.ts`, and `tests/unit/process_audit_wiring.test.ts` are the local authority.                                                                                                                                                                                                                                              |
| Delete the save-migration ladder (`D10`) | **GO; implemented in the current change**      | The owner-authorized contract is structural versioning, strict current-state proof, and a warning—not a copy migration—on content-only hash mismatch. The one-time verifier retirement record is limited to the named obsolete assets; it is not a reusable bypass.                                                                                                                                                                                                                                                                                 |
| Kill or rewire the assessor              | **REWIRE; implemented**                        | `src/afk/assessor.ts` keeps compiled hotspots as the evidence-backed input and labels a saturated 0.5 rotation as maintenance, with “no strategic recommendation” instead of presenting alphabetical order as strategy. `tests/unit/assessor.test.ts` pins the wording and classification.                                                                                                                                                                                                                                                          |
| Correct README claims                    | **Implemented**                                | `README.md` now distinguishes the procedural 247-node substrate from deeply authored Albany, describes the fixed generator skeleton and Lighthouse author fixture, qualifies validator proofs, names the real scheduled deep audit, includes UI formatting, and describes `loop.sh` as the reference driver rather than the dominant execution path.                                                                                                                                                                                                |

## Phase 3 and Phase 4 items 15–23

| Item                                                | Status                                                 | Authoritative evidence                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| --------------------------------------------------- | ------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 15 — add `variants[] / when:` for NPCs and dialogue | **Implemented**                                        | `NpcVariantSchema` and `DialogueNodeVariantSchema` in `src/rpg/schema.ts`; state resolution in `src/rpg/model.ts`; validator shadowing, unsatisfiable-condition, reference, and liveness checks in `src/validate/rpg_foundation_validator.ts`; `tests/regression/dialogue_reactive_npc_text.test.ts`; `tests/regression/rpg_variant_liveness.test.ts`.                                                                                                               |
| 16 — consolidate four Junes                         | **Implemented**                                        | One `june_pike` definition with state-reactive room/dialogue roots in `content/rpg/quests/wolf_winter.yaml`; typed TALK and save/restore evidence in `tests/regression/rpg_talk_visible_npc_resolution.test.ts`; the old ids occur only in the historical bug narrative.                                                                                                                                                                                             |
| 17 — return dialogue overlay prose to content       | **Implemented**                                        | The former engine dialogue overlay is deleted; authored variants live in Wolf-Winter YAML; metamorphic relabel and dialogue-surface tests cover the content-owned result.                                                                                                                                                                                                                                                                                            |
| 18 — make reveal receipt snapshot-derived           | **Implemented**                                        | `inspectedStoryReveals` is session state, validated on restore, and used by the one session-owned legality API; see `N7` above.                                                                                                                                                                                                                                                                                                                                      |
| 19 — move properties off the toy demo               | **Implemented**                                        | `tests/property/determinism.test.ts` runs pinned fast-check properties over shipped `sunken_barrow`; `tests/property/overworld_determinism.test.ts` covers deterministic overworld walks and repeated snapshot restoration. Both pin `numRuns`.                                                                                                                                                                                                                      |
| 20 — cover orphan validator codes                   | **Implemented**                                        | `tests/regression/rpg_foundation_negative_corpus.test.ts` derives the required code set from validator emit sites instead of pinning fixture count. `content/broken-fixtures/foundation_*.yaml` supplies the corpus. The three explicit allowlist entries have direct witnesses in `tests/unit/rpg_ending_overrides.test.ts` and `tests/regression/rpg_object_visibility_validator.test.ts`, and the allowlist rejects stale entries.                                |
| 21 — remove solver legality redundancy              | **Implemented**                                        | `src/core/engine.ts` canonicalizes the probe once. `src/solve/legal_action_cache.ts` and `src/solve/exhaustive_endings.ts` share one per-search `WeakMap` across roll regimes. `tests/regression/exhaustive_endings_cap_backstop.test.ts` pins one enumeration per immutable state.                                                                                                                                                                                  |
| 22 — delete the migration ladder                    | **Implemented; focused proof observed**                | The structural v11/v10 contract, content-mismatch warning, deleted legacy modules/fixtures, and exact one-time retirement attestation are the `D10` evidence above.                                                                                                                                                                                                                                                                                                  |
| 23 — prevent a second capture-profile ladder        | **Decision complete; future vendor execution pending** | Existing exact Codex profiles remain frozen historical readers. No new private-byte profile may be added merely for a point release. A future authenticated rollout must first demonstrate a stable structural envelope that preserves tool allowlist, model/CLI authority, cwd/filesystem binding, terminal lifecycle, and artifact hashes. Until that evidence exists, broadening acceptance would weaken the blind boundary and is intentionally not implemented. |

## Other systemic review gaps closed

| Review observation                                    | Current evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ----------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Coverage was unmeasured; deep crawl was not scheduled | `vitest.config.ts` and `package.json` define standard-project V8 coverage. `.github/workflows/deep-audit.yml` retains coverage artifacts and runs the deep audit nightly/manual.                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Bug traces were unparsed prose                        | `scripts/verify-bug-traces.ts` validates YAML shape, unique identities, narrative essentials, and current-or-historical concrete path references; it is part of health/CI.                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Long proofs had only very wide hard timeouts          | `scripts/run-with-soft-budget.ts` records the last five green durations and warns at fixed budgets or 1.5× rolling median without changing proof exit codes, caps, or hard timeouts.                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| The loop's playtest gate accepted a nonempty file     | `scripts/verify-cycle-playtest.ts` replays the runner-published raw server JSONL, requires it to reproduce the adjacent pure V2 sidecar exactly, verifies report/receipt consistency, enforces report → evidence → sidecar ordering, and binds the clean build plus current world id/hash to provisional HEAD. `tests/unit/cycle_playtest_gate.test.ts` covers sidecar-only fabrication, stale, missing, reordered, transplanted, and mismatched evidence. This is local artifact consistency, not cryptographic authorship; that stronger claim would require an external witness or secret unavailable to the writer. |
| Agent failure/revert evidence was incomplete          | `loop.sh` propagates agent failures and cleans only full-tree untracked deltas absent at cycle start. `scripts/loop-failure-ledger.ts` keeps a bounded atomic ignored history surfaced by `loop:status`; `tests/unit/loop_failure_ledger.test.ts` and `tests/regression/loop_process_scripts.test.ts` cover it.                                                                                                                                                                                                                                                                                                         |
| `src/world/` lacked an orientation map                | [`WORLD_CONCERN_MAP.md`](./WORLD_CONCERN_MAP.md) names state ownership, legality, projections, persistence, and the quest bridge.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |

## Targeted checks observed during documentation closeout

These are the only command results asserted by this document. They do not replace
the final checklist.

- `npm run verify:opening-density` — **passed**: 732/732 word tokens and 12/12
  actionable options.
- `npm run verify:bug-traces` — **passed**: 531 YAML files and 1,796 concrete
  path references (1,029 current, 766 historical, one generated-output).
- `npm exec vitest -- run tests/unit/verifier_integrity.test.ts` — **passed**:
  71 tests.
- `npm run verify:integrity -- --against origin/main` — **passed**: the approved
  D10 retirement matched all 39 reviewed deletions with no extra or missing
  deletion. The measured corpus changed from 3,282 cases / 20,427 assertions /
  19,512 strong assertions to 3,143 / 19,654 / 18,793; the command reported no
  errors and the six expected reviewed-change warnings.

## Final verification checklist — execute before landing

The boxes remain deliberately unchecked in this committed evidence index. The
landing agent must execute each item from fresh output on the final clean
revision, then record the revision-bound results in `AI_LOOP_STATE.md` and the PR
checks. Editing this document after the provisional commit would itself change
the revision that the pure playtest is meant to attest. Do not infer completion
from the implementation tables above.

- [ ] Focused D10/current-snapshot and disclosure-aware integration tests pass.
- [ ] `npm run verify:integrity` passes in static mode.
- [ ] `npm run verify:integrity -- --against <cycle-start-or-PR-base-ref>` accepts
      only the named one-time D10 retirement and reports no other weakening.
- [ ] `npm run crawl:smoke` passes on the final implementation.
- [ ] `npm run health` passes on the final implementation.
- [ ] The worktree is clean after the provisional implementation commit.
- [ ] One canonical `npm run blind` pure journey publishes a V2 report and
      sidecar bound to that exact provisional commit.
- [ ] `npm run loop:verify-playtest` accepts the report/sidecar and exact HEAD.
- [ ] The terse `AI_LOOP_STATE.md` evidence entry is the only post-gate tracked
      change and its final commit passes the applicable integrity checks.
- [ ] The short-lived branch is pushed, its PR `verify` check is green, and the PR
      is merged to `main`.
