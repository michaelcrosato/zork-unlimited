# Current Plan

Token-small AFK-loop handoff. The durable milestone and evidence rules live in
[`STARTING_SLICE.md`](STARTING_SLICE.md); do not replace that contract during an
ultraplan.

## Cycle: 2026-07-27 — Balanced CI Test Groups

## Synthesis

The starting slice has all 12 counted causal forks, all four Wolf-Winter
resolution families, and authored jobs and events in all six Albany districts.
The remaining work is experiential consolidation: make that depth legible
without flattening choices, consequences, or counterfactuals.

`main` is merged through progressive event disclosure. Its PR exposed the
remaining CI bottleneck directly: unchanged file-count shards completed in
16m25s and 41m10s. The reviewed allocator now discovers every current and future
test dynamically and assigns each exactly once. On the exact rebased suite, its
224/215-file groups predict 4,036,770/4,034,691ms, only 0.05% apart.

Exact-clean Terra seed 72761 completed Wolf-Winter with a clean three-cast
nonviolent lure at decision 30, stayed unstuck, rated clarity/enjoyment 4/4,
ended voluntarily, and chose replay-yes. It found no bug and repeated the known
front-loaded Station terminology complaint. The tooling change cannot affect
gameplay; the canary is required independent retention evidence, not claimed
coverage of CI orchestration.

## Chosen Move

Publish measured CI balancing as one green increment. Its stale cost table can
only make future groups less even; runtime discovery remains the coverage
authority, so no test can disappear because it lacks a recorded weight.

Then rebase and land the reviewed `concise-station-delay-impact` candidate. It
answers the repeated S1 by leading only live relief and June cards with canonical
`+Xm → Ym, on time/delayed` summaries while keeping enrichment private to the
current session. Do not revive the stale all-plan monologue.

After that gameplay increment, freeze for the strict pilot, deep crawl, and
authority fleet. Preserve the reviewed single-health and fresh-feedback loop
branches, but land them after starting-slice certification: two extra
health/blind/PR cycles now would delay the milestone without accelerating its
remaining manual gates.

Transport circuit: Spark remains quarantined after matching
forbidden-wrapper/model-cache failures. Probe with at most two runs only after
the client/model fingerprint changes. Until then, use one fresh Terra canary
per normal increment and the complete configured cohort only at the milestone.

## Acceptance for the next increment

1. Only current live relief and June option cards lead with concise dispatch
   impact; stale, malformed, completed, or forged surfaces expose none.
2. Each displayed total and on-time/delayed classification equals the canonical
   post-choice dispatch window, including a valid Station round trip.
3. Full, compact/MCP, terminal, UI, restore, and clone surfaces remain aligned;
   choices, timing authority, receipts, routes, RNG, and outcomes do not change.
4. Crawl, health, one fresh pure report, independent review, and GitHub
   `verify` must be green.
