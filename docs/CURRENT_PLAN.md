# Current Plan

Token-small AFK-loop handoff. The durable milestone and evidence rules live in
[`STARTING_SLICE.md`](STARTING_SLICE.md); do not replace that contract during an
ultraplan.

## Cycle: 2026-07-27 — Relief Protocol Divergence Reminder

## Synthesis

The starting slice has all 12 counted causal forks, all four Wolf-Winter
resolution families, and authored jobs and events in all six Albany districts.
The remaining work is experiential consolidation: make that depth legible
without flattening choices, consequences, or counterfactuals.

`main` is merged through `5fce4762`, including the first-cast failure forecast.
Candidate `4f53e8af` closes the compiler's next exact hotspot: a player who
prepared Jamie's Relief Protocol but used the mutually exclusive braced-rail
recovery received no field reminder explaining why the docket stayed shut.

The Broken Paling now names the protocol's actual failed-wedge and spent
split-guard trigger, says a braced recovery leaves its docket sealed, and keeps
the existing route to the second cast. No mechanic changed. Full and compact
observations, the exact imported-state liveness route, the split-guard control,
and a non-Relief negative lock the boundary.

Exact-clean pure Terra seed 72751 chose Drover rather than Relief, completed
Wolf-Winter bloodlessly with all wolves and cattle alive at decision 30, then
continued and died in Gallowmere at decision 48. The player rated clarity and
enjoyment 4/4, stayed unstuck, and chose replay-yes. The canary therefore proves
general build health, not organic exposure of this reminder; deterministic
proofs carry the targeted claim.

## Chosen Move

Land `4f53e8af` with its exact-clean evidence note. Do not add a recap, action,
or persistent reminder; the divergence copy belongs only at the field state
where the selected protocol becomes unavailable.

Next, fix seed 72751's authenticated dispatch presentation trace. After the
optional allocation the card said `65m—delayed`; talking to June opened her
field-team choice and collapsed the card to `unverified—neutral`; selecting her
restored `80m—delayed`. The journal proof is behaving safely, but a pending
current decision is not legacy state. Represent it as an explicit provisional
dispatch status and tell the player that the final total awaits the field-team
choice. Never claim neutral/no pressure when the completed ledger already
proves delay.

Transport circuit: Spark remains quarantined after matching
forbidden-wrapper/model-cache failures. Probe with at most two runs only after
the client/model fingerprint changes; stop on two matching infrastructure
failures. Until then, use one fresh Terra canary per normal increment and
larger cohorts only for milestone re-aims.

## Acceptance for the next increment

1. The exact delayed → pending-June → delayed trace never displays
   `legacy_neutral`, `unverified`, or `no pressure` during the current decision.
2. The provisional state is authenticated from the same journal boundaries and
   cannot alter launch legality, elapsed minutes, the final receipt, or pressure.
3. Solo, June-selected, closed/unassigned, legacy-save, tampered-journal, full,
   compact, UI, CLI, and MCP counterfactuals remain truthful.
4. Both crawl gates, full health, independent review, and one exact-clean pure
   blind pass are green before landing.
