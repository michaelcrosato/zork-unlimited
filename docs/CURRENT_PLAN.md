# Current Plan

Token-small AFK-loop handoff. The durable milestone and evidence rules live in
[`STARTING_SLICE.md`](STARTING_SLICE.md); do not replace that contract during an
ultraplan.

## Cycle: 2026-07-15 — Wolf-Winter Hill Approach

## Synthesis

`SS-F07-hill-route` is now an authored quest-launch fork rather than an
off-map teleport. Hayden presents two exact last-mile commitments on the
Station Quarter quest card. The exposed ridge spends 30 minutes, one supply,
and 25 fatigue; it starts cattle alarm at 1 but makes the first feed cast DC 10.
The sheltered stockway spends 75 minutes, two supplies, and 10 fatigue; it
starts the herd calm but leaves the first cast at DC 12. Each option shows its
actual cost, projected arrival, resulting travel condition, known field risk,
and blocked reason before the same button starts the quest.

The generic launch boundary prepares the post-choice character and resources
before the embedded RPG boots, then commits one suffixed quest-start decision.
It imports exactly one route flag, exposes exactly one last-mile action, records
Hayden's route memory, and fails closed if both route flags appear. A seed-9
pair makes the ridge cast pass while the stockway cast fails into the existing
no-retry rail recovery. A seed-26 pair makes both casts pass, but the ridge
reaches alarm 4 and scatters cattle while the stockway reaches alarm 3 and keeps
the herd whole. Both remain viable, and all four resolution families remain
reachable.

Approach cost, proof, relationship memory, RPG import receipt, quest outcome,
and exact return summary survive chronological foldback, current snapshot
replay, and exact F11 migration. Full/compact MCP, browser, CLI, crawler, and
spectator surfaces share the choice without exposing internal effects. The
route replaces the old first local movement input, so the typical first-goal
decision ceiling does not grow.

The matrix honestly advances to 10/12 material forks and remains at 4/4
resolution families. The greater-than-BG3 slice claim remains
`active_unproven`: `SS-F06-relief-allocation`, `SS-F02-relief-oath`, the complete
numeric audit, fleet-level balance, and publication remain outstanding.

## Chosen Move

After the hill-approach increment lands green, implement
`SS-F06-relief-allocation` as the next bounded fork.

- Reuse Albany's existing relief, relationship, time, service, and journey
  primitives; do not add a new town, quest, or decorative reserve meter.
- Present one finite allocation before departure whose alternatives name what
  they can protect and what remains exposed.
- Make the selected allocation alter at least one Wolf-Winter starting
  condition or failure recovery and one truthful Albany return service, price,
  or availability state.
- Preserve every existing combat, lure, drive, and fortify route and the
  first-goal ceiling of 45 meaningful decisions.
- Keep `SS-F02-relief-oath` as the final material fork; do not claim slice
  certification until both rows and every numeric Depth Contract threshold are
  machine-proven.

## Acceptance for the next increment

1. `SS-F06-relief-allocation` becomes one reachable, irreversible paired
   counterfactual with disclosed finite cost, a delayed mechanical consumer,
   and truthful feedback at allocation, Wolf arrival, and Albany return.
2. Fixed reachable states prove that different allocations are advantageous
   under different conditions; no option dominates every strategy or build.
3. Save/replay, chronological foldback, full/compact/UI parity, and exact
   predecessor migration reject invented, contradictory, or relabelled
   allocation evidence.
4. All four Wolf-Winter families remain reachable, score-sound, and within the
   existing liveness/search and first-goal ceilings.
5. Only proven rows advance; pre/post crawl, one fresh canonical pure exit,
   independent audit, and `npm run health` remain mandatory.
