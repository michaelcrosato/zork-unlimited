# Current Plan

Token-small AFK-loop handoff. The durable milestone and evidence rules live in
[`STARTING_SLICE.md`](STARTING_SLICE.md); do not replace that contract during an
ultraplan.

## Cycle: 2026-07-16 — June Nonlethal Combat Boundary

## Synthesis

The homogeneous-Sonnet authority contract is merged. Its final pure player was
the third consecutive report that accepted June Pike's cattle-first,
no-wolf-killing agreement and then read Cade's guarded rail as a defensive or
living tactic. In reality, crossing the paling without a named strategy silently
retired lure, drive, and fortification and committed hunt-and-hold combat. A
fouled committed lure had a second inversion: a failed rail could be bound into
a living recovery, while a successful brace itself supplied no living rail
recovery.

This increment repairs that causal contract:

- June holds the final uncommitted crossing and explicitly names the ordinary
  rail as a spear-fighting funnel whose first wolf death ends her agreement;
- keeping cattle-first terms leaves lure, drive, and fortification open, while
  explicit acknowledgement alone opens the ordinary combat crossing;
- after a fouled committed lure, either a successful brace or the existing
  failed-rail bind can become a target-only scent-pen that redirects the
  yearling alive with distinct provenance and no new retry or free alarm relief;
- choosing the guarded spear maneuver explicitly and irreversibly retires every
  living recovery, but June's promise breaks only if the wolf actually dies;
- solo controls, Reese preparation variants, restored saves, chronological
  foldback, June memory/promise/companion state, Station service, compact/full
  MCP, and UI surfaces agree.

Final-world verifier-valid pure Spark seed 2850 completed the
all-three-wolves-alive lure, continued twice, completed The Gallowmere, and
ended honestly at decision 58 with clarity/enjoyment 4/4, no stuck state,
replay intent, and no declared bug. Its evidence binds the corrected `696049…`
world manifest. Same-origin deterministic pairs carry the repaired failed-lure,
hybrid, and June-return branches the player did not choose. Earlier seed 2825
remains useful pre-copy diagnostics, not final-world evidence.

Four fixed ten-Spark diagnostics, with no retries, produced 30/40 valid fresh
pure exits. Valid runs alone used 167,556,799 input tokens (162,006,912 cached)
and 1,100,087 output tokens; the measured Spark meter moved from 100% to 34%.
The cohort included 124-, 120-, and 80-decision journeys plus broad Wolf ending
coverage. Rejected runs preserve tool-domain confusion, lost parent handles,
and two explicit Codex context-compactor failures. The journey chooser works
when the parent handle survives, but embedded quest responses expose only the
child RPG handle and parent snapshot hash. This is a real
playtest-recoverability gap, not evidence that the June fix failed.

The greater-than-BG3 milestone remains `active_unproven`. The current Spark
cohort is diagnostic, not the authenticated Sonnet pilot or authority bundle.
The objective audit also confirms that generic one-click jobs still survive in
all six Albany districts, contradicting the no-generic-job depth contract.
Global authored failure-forward proof, three independent NPC agendas, a current
deep crawl, the clean ten-player pilot, and exact 100-player certification also
remain open.

## Chosen Move

Land this isolated June content increment with its canonical report and green
repository/GitHub gates. On clean `main`, make the next increment repair the
blind-play transport contract: echo `overworld_session_id` through every
embedded quest response and pause, distinguish parent/child session arguments
and wrong-domain errors, and make duplicate-start recovery return the already
bound parent handle. Prove a player can continue a checkpoint using only the
latest embedded response after deliberately discarding the original start
response.

Then run fresh Spark cohorts without retrying failed seeds while development
continues. Once transport validity is stable, the next gameplay move is to
replace Albany's generic one-click district jobs with authored scene grammar and
material delayed consequences; persistent hunt-and-hold attrition is the next
strong Wolf-return consumer. Reassess before paying for the clean Sonnet pilot,
deep crawl, and exact 100-player authority cohort.

## Acceptance for this increment

1. June discloses the combat boundary before an uncommitted crossing without
   removing any living strategy commitment.
2. Successful and failed rails after the same fouled committed lure both expose
   one bounded living scent recovery; ordinary rail play remains combat-only.
3. The first hybrid spear choice permanently closes every living recovery, and
   June's promise changes only on actual wolf death.
4. Same-origin counterfactuals cover preparation, solo control, save/restore,
   foldback, service, compact/full MCP, UI, and the decision budget.
5. Post-change crawl, one canonical pure report, focused proofs, full health,
   required GitHub checks, and squash merge are green before starting the
   separate session-recovery increment.
