# Current Plan

Token-small AFK-loop handoff. The durable milestone and evidence rules live in
[`STARTING_SLICE.md`](STARTING_SLICE.md); do not replace that contract during an
ultraplan.

## Cycle: 2026-07-25 — Comparison-First Albany Departure

## Synthesis

The starting slice has all 12 counted causal forks, all four Wolf-Winter
resolution families, and authored jobs and events in all six Albany districts.
Its remaining work is experiential consolidation: the opening must make those
systems legible without flattening their consequences.

Three recent exact-build Terra journeys independently chose nearly the same
Road-Warden, aid-only, Hayden, Drover, June, fodder, exposed-ridge, bloodless
lure route. All completed cleanly, but all identified the consecutive Albany
planning sequence as dense. A baseline of the six decision payloads on that
path measured 4,191 `o200k_base` tokens; 1,029 tokens were sibling consequence
prose shipped before the player asked to inspect any one option.

The focused increment converts Registration, Relief Oath, Lead Source,
Preparation, June, and Relief Allocation to one truthful comparison contract:
commitment, field trigger, immediate cost, and tradeoff remain visible for every
option. Compact active choices stage their complete terms behind the existing
read-only exact-option inspection. Full developer output and the human UI retain
the canonical terms, and all legal choices and mechanics are unchanged.

The same measured path now costs 3,558 always-visible tokens, down 633 or 15.1%.
Including the one exact Drover inspection the recent player actually requested,
the comparable total falls from 4,992 to 4,308, down 13.7%. Selected-detail
inspection still repeats comparison context; making that response detail-only
is the next bounded token-efficiency candidate, not part of this increment.

## Chosen Move

Land the comparison-first journey contract and remove the duplicated Station
mission/route preview. Preserve immediate-predecessor saves because the manifest
change is presentational: exact predecessor snapshots rebind to the current
world hash only after the normal replay and integrity checks pass.

Do not spend a ten- or 100-player fleet on a known common-mode presentation
bias. Finish this increment with one fresh exact blind. After it lands, use a
small homogeneous pilot to decide whether the larger current-build cohorts are
worth the spend while ordinary development continues in parallel.

## Acceptance for this increment

1. All six Albany decision kinds expose non-empty commitment, trigger, cost, and
   tradeoff fields; malformed authoring fails closed.
2. Every compact active option stages its long consequence, including
   summaryless post-goal choices. Inspecting one exact option returns its
   canonical detail without sibling leakage or state mutation.
3. Full output, UI, terminal, compact MCP, and pure-player guidance agree on the
   contract; direct and inspected choices produce identical state and hashes.
4. The exact immediate-predecessor manifest restores without gameplay changes,
   while unrelated hashes and tampered state remain rejected.
5. Focused proofs, post-change crawl, full health, and one verifier-valid fresh
   blind pass before the green increment is recorded and committed.
