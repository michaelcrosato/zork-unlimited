# Current Plan

Token-small AFK-loop handoff. The durable milestone and evidence rules live in
[`STARTING_SLICE.md`](STARTING_SLICE.md); do not replace that contract during an
ultraplan.

## Cycle: 2026-07-19 — Wolf-Winter Crawlboard Source Truth

## Synthesis

A verifier-valid Terra player correctly found a causal-copy defect, not a state
leak. Jamie Tanner's certified testimony and Cade's committed feed plan both use
the same physical feed-hauler crawlboard, but for different purposes. Jamie
alone opens the uncommitted rail-free combat drop after the yearling falls.
Cade can independently teach that board only as the second cast of his
irreversible finite-feed plan. Cade's instruction must not imply that Albany
bought Jamie's evidence, and it must not grant Jamie's combat opening.

The repaired copy now names those sources where each is earned. Generic store
and blocked-exit text no longer says the crawlboard is “in your packet.”
Mechanics are unchanged: Hayden plus ordinary combat remains barred; Hayden plus
Cade's committed lure can cross for the second cast without receiving Jamie's
flag, memory, or prose. Source, strategy, loft-resource, compact-surface, and
full-observation counterfactuals prove all three cases. The defining-slice docs
and causal matrix state the same boundary.

The first full-health run caught four overlong replacements before landing.
They were shortened without relaxing compact limits. The exact rebased tree
includes the merged Civic Winter Return Docket and passes 384/384 test files
and 2,926/2,926 tests, plus integrity, typecheck, lint, format, UI typecheck,
and all 12 packs. Final crawl `20260719T132548Z` has zero findings at 247/247
nodes, 344/344 edges, and 12/12 boards/quests. Independent red-team found no
P0–P2 issue.

Fresh Terra seed 4116 is verifier-valid: 46 decisions, Wolf-Winter
fortification plus The Gallowmere, clarity/enjoyment 4/4, unstuck, replay yes.
It saw the corrected generic hint but did not choose Jamie or Cade's lure, so it
does not substitute for deterministic paired proof. The clean ten-Spark fleet
on commit `1be6fec4` used seeds 4120–4129 with concurrency four, no resume,
and no retry. One member verified: seed 4122 completed Wolf-Winter at decision
23, rated 4/5, was unstuck, and would replay. It chose Hayden and plain combat,
so it also did not traverse the repaired route. Nine members failed the strict
pure envelope after forbidden resource or patch-tool use; none are counted.
The cohort consumed 59.48M input tokens (57.74M cached), 312.65K output, and
211.42K reasoning. Rejected seed 4121 did organically complete Cade's local
crawlboard line as Pack Diverted, but that remains exploratory only; no member
used Jamie's route. The bundle is valuable provider-compliance and non-counting
open-play diagnostic evidence, not direct retention proof for the copy.

Feedback compile `20260719T130217Z` has 833 verified reports, 82
current-contract pure exits, and 68 players who continued at least once. Seed
4122 is the sole new accepted report since that compile, so another compile is
not due. The prior valid report's contact-id complaint was player misuse; its
fixed checkpoint was disclosed pacing. The child quest surface does have a
separate low-severity naming inconsistency: responses say `rpg_session_id`,
while tool arguments use the generic key `session_id`.

The generic assessor rotates to structurally clean `advocates_case` at score
0.5 because every shipped quest validates. Its compiled notice-board
recommendation is the already-diagnosed stale structural-fleet cluster, not
fresh starting-slice evidence. The user's defining-slice contract therefore
keeps priority on authored Albany depth.

Civic is merged as `6a9fc87f`. Albany now has 2/6 authored jobs and 1/6
authored events. The greater-than-BG3 milestone remains `active_unproven`.

## Chosen Move

Finish the copy branch's final-tree health/crawl, push it through a short PR,
and merge only after GitHub `verify` and `crawl-smoke` pass. Campus Archive
Query / Blair is already developing in a separate worktree so development and
blind testing remain parallel.

Campus is post-Wolf and optional, adding zero direct first-goal decisions.
Blair asks for either a fast, confidence-labelled road warning or a slower,
traceable field archive. Both are responsible evidence products; the tradeoff
is immediate operational usefulness versus reproducible provenance, not
Civic's public-versus-protected legal-policy choice. Each unlocks a distinct
one-use Campus service through exact replay-bound job proof.

## Acceptance for this increment

1. Jamie's certified combat opening, Cade's committed second cast, and
   Hayden's barred combat route are causally distinct in every player surface.
2. No source flag, route, resource, ending, score, save, or replay mechanic
   changes.
3. Compact copy is complete and untruncated; deterministic pairs, fresh
   verified journeys, final crawl, and full health stay green.
4. Both required GitHub checks pass before merge; rejected Spark runs never
   enter the ledger.
