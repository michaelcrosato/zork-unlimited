# Current Plan

Token-small AFK-loop handoff. The durable milestone and evidence rules live in
[`STARTING_SLICE.md`](STARTING_SLICE.md); do not replace that contract during an
ultraplan.

## Cycle: 2026-07-27 — Compact Read-Only Option Review

## Synthesis

The starting slice has all 12 counted causal forks, all four Wolf-Winter
resolution families, and authored jobs and events in all six Albany districts.
The remaining work is experiential consolidation: make that depth legible
without flattening choices, consequences, or counterfactuals.

`main` is merged through `76678085`, including optional Station departure
staging. Candidate `1cd64404` addresses the remaining transport-level
discoverability gap: compact comparisons already supported exact read-only
option detail, but the executable `option_id` path was buried in the global
tool description.

Comparison contract v4 now returns one `reviewOption` action beside the cards.
It names the exact inspection tool, story id, snake-case argument body,
`option_id` value source, and `readOnly: true`. Constructing that action from
the response preserves the snapshot, exported save, decisions, time, money,
and sibling-term isolation. Full/UI terms and the exact-detail payload are
unchanged.

Fresh pure Terra seed 72748 received v4 on both Station comparisons but did not
use either review action. It completed The Wolf-Winter nonlethally with two
cattle lost at decision 33, rated clarity/enjoyment 4/4, stayed unstuck, and
chose replay-yes. It still described the optional setup as front-loaded, so the
new capability is truthful but did not solve this player's cognitive load.

## Chosen Move

Land `1cd64404` with this evidence note. Do not add another review hint or expand
the recap: the canary saw the executable affordance and chose from summaries
anyway.

Next, forecast cumulative first-cast failure pressure on the existing Broken
Paling route card. Seed 72748 understood the clean route but only understood the
combined cattle-alarm cost after its failed cast. Add one tailored sentence
that states arrival alarm and the total after a fouled first cast, including
delayed-dispatch pressure, while retaining every existing recovery.

Prove the forecast against deterministic failed-first-cast traces for both hill
routes and the relevant preparation/allocation/dispatch states. This is
presentation-only: no RNG, alarm mutation, action, retry, cost, route, or ending
changes.

Transport circuit: Spark remains quarantined after matching
forbidden-wrapper/model-cache failures. Probe with at most two runs only after
the client/model fingerprint changes; stop on two matching infrastructure
failures. Until then, use one fresh Terra canary per normal increment and
larger cohorts only for milestone re-aims.

## Acceptance for the landed increment

1. Every compact comparison exposes an executable exact-option review action
   adjacent to its option ids and explicitly marks it read-only.
2. The returned argument body can be used directly with one selected option id;
   exact detail exposes no sibling terms.
3. Review preserves snapshot hash, export, decisions, time, money, and the
   subsequent direct-choice result; full/UI behavior is unchanged.
4. Comparison contract versioning moves honestly while exact-detail shape stays
   stable for consumers.
5. Focused regressions, both crawl gates, final full health, independent review,
   and one verifier-valid fresh blind pass are green before publication.
