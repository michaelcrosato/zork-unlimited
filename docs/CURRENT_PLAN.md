# Current Plan

Token-small AFK-loop handoff. The durable milestone and evidence rules live in
[`STARTING_SLICE.md`](STARTING_SLICE.md); do not replace that contract during an
ultraplan.

## Cycle: 2026-07-27 — Concise Station Delay Impact

## Synthesis

The starting slice has all 12 counted causal forks, all four Wolf-Winter
resolution families, and authored jobs and events in all six Albany districts.
The remaining work is experiential consolidation: make that depth legible
without flattening choices, consequences, or counterfactuals.

`main` is merged through balanced CI test groups. PR 195's live groups completed
in 38m05s/38m44s, replacing the prior 16m25s/41m10s tail with a 39-second gap
while dynamically covering every test exactly once.

The final Station candidate now leads only live relief-allocation and June cards
with a concise canonical added-delay, resulting-minute, and on-time/delayed
summary. Enrichment is private to the current `OverworldSession`; public code can
remove stale impact but cannot mint it. Stale, malformed, forged, completed, and
non-Station surfaces fail closed. Mechanics and outcomes are unchanged.

Exact-clean Terra seed 72762 organically chose fodder and June cattle-first,
recovered a failed first lure through Emery's route, completed Wolf-Winter at
decision 33, and continued to checkpoint 40. It stayed unstuck, rated
clarity/enjoyment 4/4, and chose replay-yes. It said the staged details helped
but still raised the broader opening stack as S1 density.

## Chosen Move

Publish concise Station delay impact as the final pre-pilot gameplay increment.
Do not treat one 4/4 canary as closure and do not revive the stale all-plan
monologue; the canary's honest S1 now belongs in the cohort measurement.

After GitHub `verify` merges this exact patch, freeze the tracked build. Run the
fresh strict homogeneous ten-player Terra pilot and the 120-second, eight-worker
deep crawl in parallel. Validate the closed no-resume/no-retry fleet with
`starting-slice:pilot`. If it misses a quality gate, adapt only from meaningful
cohort evidence; if it passes, launch the fresh strict 100-player Terra authority
fleet and certify it.

Preserve the reviewed single-health and fresh-feedback loop branches, but land
them after starting-slice certification: they do not accelerate these remaining
manual milestone gates.

Transport circuit: Spark remains quarantined after matching
forbidden-wrapper/model-cache failures. Probe with at most two runs only after
the client/model fingerprint changes. Until then, use one fresh Terra canary
per normal increment and the complete configured cohort only at the milestone.

## Acceptance for the next increment

1. Pilot preflight freezes one exact clean merged commit, world id/hash, Codex
   client, Terra model, default player, and ten fresh contiguous seeds.
2. All ten runs finish once with primary subjective reports, no resume, retry,
   failed attempt, or model-assisted recovery, and unique game/provider sessions.
3. The pilot passes all completion, decision-depth, clarity, enjoyment,
   continuation, stuck, severity, and strategy-diversity gates.
4. `npm run crawl:deep -- --seconds 120 --workers 8` produces no blocking
   finding on the same frozen build.
5. Only a passing pilot authorizes the fresh strict 100-player Terra fleet and
   `starting-slice:certify`.
