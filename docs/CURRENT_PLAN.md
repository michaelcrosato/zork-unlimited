# Current Plan

Token-small AFK-loop handoff. The durable milestone and evidence rules live in
[`STARTING_SLICE.md`](STARTING_SLICE.md); do not replace that contract during an
ultraplan.

## Cycle: 2026-07-27 — First-Cast Failure Forecast

## Synthesis

The starting slice has all 12 counted causal forks, all four Wolf-Winter
resolution families, and authored jobs and events in all six Albany districts.
The remaining work is experiential consolidation: make that depth legible
without flattening choices, consequences, or counterfactuals.

`main` is merged through `b177e6d5`, including executable read-only option
review. Candidate `383ff926` closes the next concrete information gap: hill
cards previously projected only a clean lure while the first failed cast could
combine route arrival, full failed-cast pressure, and dispatch delay.

Both roads now name the cumulative failed-first-cast alarm before commitment:
ridge/stockway 3/2 on time and 4/3 when dispatch is delayed. The same sentence
states that feed is spent, there is no retry, and recovery remains. Compiled
worst-cast traces prove those totals and the legal recovery against every
presentation-relevant Cade-fodder, Aid-Only, road, and delay combination. No
mechanic or action changed; the longest legal card is 354/360 characters.

Exact-clean pure Terra seed 72750 chose the exposed road for its clearer DC 10,
then suffered the forecasted failed cast and used the surviving rail recovery.
Every wolf and cattle survived; Wolf-Winter completed at decision 30, the
player continued through Gallowmere to 54, rated clarity/enjoyment 4/4, stayed
unstuck, and chose replay-yes.

## Chosen Move

Land `383ff926` with the exact-clean evidence note. Do not add more hill-card
copy: the forecast was visible, its consequence occurred, and recovery remained
legible enough to produce the bloodless ending.

Next, assess the compiler's new top hotspot against the broader presentation
problem. Seed 72750 lost track of why Jamie's Relief Protocol did not appear
after the braced-rail recovery; the protocol intentionally triggers only after
the split-guard line. Prefer a small field-state reminder at the divergence if
it can name why the protocol is unavailable without turning every room into a
recap. Compare that focused move with the repeated complaint that compact state
payloads obscure immediate choices before selecting the next increment.

Transport circuit: Spark remains quarantined after matching
forbidden-wrapper/model-cache failures. Probe with at most two runs only after
the client/model fingerprint changes; stop on two matching infrastructure
failures. Until then, use one fresh Terra canary per normal increment and
larger cohorts only for milestone re-aims.

## Acceptance for the landed increment

1. Every hill card names the runtime-true failed-first-cast alarm total before
   route commitment, including delayed-dispatch pressure.
2. Finite feed, no retry, and retained recovery are explicit without changing
   mechanics, legality, costs, or outcomes.
3. Full, compact, MCP, UI, and CLI surfaces remain exact and every legal
   knowledge/dispatch projection stays within 360 characters.
4. Compiled worst-cast traces, both crawl gates, full health, independent
   review, and one exact-clean verified pure blind pass are green.
