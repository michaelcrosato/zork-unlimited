# Current Plan

Token-small AFK-loop handoff. The durable milestone and evidence rules live in
[`STARTING_SLICE.md`](STARTING_SLICE.md); do not replace that contract during an
ultraplan.

## Cycle: 2026-07-27 — Preparation Dispatch Forecast

## Synthesis

The starting slice has all 12 counted causal forks, all four Wolf-Winter
resolution families, and authored jobs and events in all six Albany districts.
The remaining work is experiential consolidation: make that depth legible
without flattening choices, consequences, or counterfactuals.

`main` is merged through the selected-term recap. The two-shard CI pilot cut
required-check wall time from a recent 70m05s average to 45m21s without
dropping a test or gate. Keep the pilot for feature landings and gather more
timings before changing shard count or balancing.

Three new Terra reports compiled as verified pure evidence with zero rejection.
They retain 100% replay intent and no stuck players, while isolating Albany
choice density, cumulative Station timing, one later Gallowmere lethal check,
and one later Queensbury identity ambiguity. The latter two are outside this
fixed starting slice.

The preparation forecast rebased with an identical patch id. Every preparation
card now shows its authenticated final dispatch range across remaining optional
allocation and field-team time, while the route card and final receipt remain
authoritative for the actual launch. Exact-clean seed 72755 saw all three
delayed ranges, completed Wolf-Winter at decision 21, rated clarity/enjoyment
3/4, stayed unstuck, and chose replay-yes.

## Chosen Move

Publish the preparation forecast as one green increment. Do not rerun the blind
player merely because it did not name the forecast; deterministic proofs cover
all option/range combinations, and the report confirms no mechanical
regression.

After this merges, rebase the concise Civic first-use-category increment
directly onto `main`, run one fresh Terra canary, and land it independently.
Exact Wolf-Winter completion guidance follows. A separate tooling increment
then removes the autonomous loop's duplicate in-agent full-health run while
retaining `loop.sh` as the sole authoritative health gate.

Transport circuit: Spark remains quarantined after matching
forbidden-wrapper/model-cache failures. Probe with at most two runs only after
the client/model fingerprint changes. Until then, use one fresh Terra canary
per normal increment and the complete configured cohort only at the milestone.

## Acceptance for the next increment

1. Every Civic role, duty, and evidence card exposes one optional concise
   first-use category while exact inspectable terms remain complete.
2. Legacy manifests with all categories omitted keep their prior presentation;
   partial, blank, overlong, stale, or tampered category data fails closed.
3. Full terminal, UI, compact, and MCP comparisons stay in parity, and the
   selected-term recap may reuse only authenticated selected categories.
4. Mechanics, choices, costs, effects, receipts, RNG, routes, and endings remain
   unchanged; crawl, health, one fresh pure report, review, and GitHub `verify`
   must be green.
