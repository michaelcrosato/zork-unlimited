Scale-out Phase 1: parallel agent lanes + cheap persona QA fleet

Strategic decision (spec: docs/superpowers/specs/2026-08-27-scaleout-design.md):
keep the certified core untouched; restructure the coordination layer
additively. Evidence: 695 commits in July vs 57 in August with two
landings after Aug 12; pilots stuck at clarity 38-40/50 while the
certifier demands strategy diversity the harness bans its only lever
for (personas locked out of live runs); every loop coordination file
is fixed-path last-writer-wins.

Pillar 1 — lanes: scripts/lane.mjs + docs/parallel_lanes.md; four
worktree lanes with zones and a single-writer rule for the overworld
JSON, AI_LOOP_STATE.md, DECISION_LOG.md, and traces/bugs sequence
numbers. Landing bar unchanged (PR + verify).

Pillar 2 — QA fleet: blind-tester/qa/qa-fleet.mjs (npm run qa:fleet):
cheap instant tier (spark @ low effort), seeded 1-in-100 max-think
upgrades (terra @ xhigh), critical-leaning persona roster (new critic/
skeptic/impatient + breaker weighted 9/13 of the cycle), server-side
blindness via --play-mode pure, output quarantined under ai-runs/qa/
so pure-lane attendance, pilots, certification, and the acceptance
chain never see it. Advisory evidence only; docs/qa_fleet.md.

Verification: lint, typecheck, prettier, and the 12 new unit tests are
green; health's fast gates (integrity, bug traces, opening density,
format) passed. The full 4,104-test suite run was aborted under heavy
machine contention — CI verify remains the landing arbiter for the PR.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
